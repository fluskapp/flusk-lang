name: verifyCredentials
inputs:
  - name: email
    type: string
  - name: password
    type: string
output:
  type: object
  fields:
    - token
    - member
    - org
steps:
  - id: execute
    action: return
    value:
      ok: true
