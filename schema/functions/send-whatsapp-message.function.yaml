name: sendWhatsappMessage
description: Send a message via WhatsApp provider
inputs:
  - name: phone
    type: string
  - name: message
    type: string
  - name: bot_id
    type: string
output:
  type: object
steps:
  - id: execute
    action: return
    value:
      ok: true
