name: setupDefaultTools
description: Pre-populate known AI tools for a new org
inputs:
  - name: org_id
    type: integer
output:
  type: object
steps:
  - id: execute
    action: return
    value:
      ok: true
