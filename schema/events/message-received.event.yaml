name: MessageReceived
channel: kafka
payload:
  fields:
    - name: bot_id
      type: string
    - name: conversation_id
      type: string
    - name: message_id
      type: string
    - name: contact_phone
      type: string
