name: alerts
description: List open alerts for an organization
args:
  - name: org
    type: string
    required: true
  - name: status
    type: string
    required: false
action:
  call: listAlerts
  with:
    org: org
    status: status
