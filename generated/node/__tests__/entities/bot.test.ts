// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { BotRepository } from '../../src/repositories/bot.repository.js';

describe('BotRepository', () => {
  let db: Database.Database;
  let repo: BotRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE bots (id TEXT PRIMARY KEY, name TEXT NOT NULL, phone_number TEXT NOT NULL, system_prompt TEXT NOT NULL, model TEXT NOT NULL, temperature REAL NOT NULL, max_tokens INTEGER NOT NULL, active INTEGER NOT NULL, owner_id TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new BotRepository(db);
  });

  const testData = () => ({
    name: 'test-name',
    phone_number: 'test-phone_number',
    system_prompt: 'test-system_prompt',
    model: 'gpt-4o',
    temperature: 3.14,
    max_tokens: 42,
    active: true,
    owner_id: 'test-owner_id',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { name: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
