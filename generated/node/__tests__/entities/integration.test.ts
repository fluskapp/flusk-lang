// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { IntegrationRepository } from '../../src/repositories/integration.repository.js';

describe('IntegrationRepository', () => {
  let db: Database.Database;
  let repo: IntegrationRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE integrations (id TEXT PRIMARY KEY, assistant_id INTEGER NOT NULL, provider TEXT NOT NULL, scopes TEXT NOT NULL, access_token_ref TEXT NOT NULL, refresh_token_ref TEXT, token_expires_at TEXT, status TEXT NOT NULL, last_used_at TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new IntegrationRepository(db);
  });

  const testData = () => ({
    assistant_id: 42,
    provider: 'google',
    scopes: { key: 'value' },
    access_token_ref: 'test-access_token_ref',
    status: 'active',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { assistant_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
