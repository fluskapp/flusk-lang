// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { TelemetryEventRepository } from '../../src/repositories/telemetryevent.repository.js';

describe('TelemetryEventRepository', () => {
  let db: Database.Database;
  let repo: TelemetryEventRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE telemetry_events (id TEXT PRIMARY KEY, org_id INTEGER NOT NULL, device_id TEXT NOT NULL, trace_id TEXT NOT NULL, span_id TEXT NOT NULL, provider TEXT NOT NULL, model TEXT NOT NULL, tool_name TEXT, input_tokens INTEGER NOT NULL, output_tokens INTEGER NOT NULL, latency_ms INTEGER NOT NULL, cost_usd REAL NOT NULL, status_code INTEGER NOT NULL, is_approved INTEGER NOT NULL, metadata TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new TelemetryEventRepository(db);
  });

  const testData = () => ({
    org_id: 42,
    device_id: 'test-device_id',
    trace_id: 'test-trace_id',
    span_id: 'test-span_id',
    provider: 'test-provider',
    model: 'test-model',
    input_tokens: 42,
    output_tokens: 42,
    latency_ms: 42,
    cost_usd: 3.14,
    status_code: 42,
    is_approved: true,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { org_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
