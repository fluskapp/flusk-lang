// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AiToolRepository } from '../../src/repositories/aitool.repository.js';

describe('AiToolRepository', () => {
  let db: Database.Database;
  let repo: AiToolRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE ai_tools (id TEXT PRIMARY KEY, org_id INTEGER NOT NULL, name TEXT NOT NULL, provider TEXT NOT NULL, category TEXT NOT NULL, approved INTEGER NOT NULL, detection_pattern TEXT, first_seen_at TEXT NOT NULL, last_seen_at TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AiToolRepository(db);
  });

  const testData = () => ({
    org_id: 42,
    name: 'test-name',
    provider: 'test-provider',
    category: 'chat',
    approved: true,
    first_seen_at: '2026-01-01T00:00:00.000Z',
    last_seen_at: '2026-01-01T00:00:00.000Z',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { org_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
