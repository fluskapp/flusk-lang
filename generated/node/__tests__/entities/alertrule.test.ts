// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AlertRuleRepository } from '../../src/repositories/alertrule.repository.js';

describe('AlertRuleRepository', () => {
  let db: Database.Database;
  let repo: AlertRuleRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE alert_rules (id TEXT PRIMARY KEY, org_id INTEGER NOT NULL, name TEXT NOT NULL, type TEXT NOT NULL, threshold TEXT, channels TEXT NOT NULL, enabled INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AlertRuleRepository(db);
  });

  const testData = () => ({
    org_id: 42,
    name: 'test-name',
    type: 'shadow-ai',
    channels: { key: 'value' },
    enabled: true,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { org_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
