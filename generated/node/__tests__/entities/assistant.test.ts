// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AssistantRepository } from '../../src/repositories/assistant.repository.js';

describe('AssistantRepository', () => {
  let db: Database.Database;
  let repo: AssistantRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE assistants (id TEXT PRIMARY KEY, account_id INTEGER NOT NULL, user_id INTEGER, name TEXT NOT NULL, slug TEXT NOT NULL, soul TEXT, identity TEXT, user_context TEXT, memory TEXT, llm_provider TEXT NOT NULL, llm_model TEXT NOT NULL, temperature REAL NOT NULL, machine_tier TEXT NOT NULL, container_id TEXT, container_status TEXT NOT NULL, openclaw_version TEXT NOT NULL, tools_enabled TEXT NOT NULL, sandbox_level TEXT NOT NULL, status TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AssistantRepository(db);
  });

  const testData = () => ({
    account_id: 42,
    name: 'test-name',
    slug: 'test-slug',
    llm_provider: 'anthropic',
    llm_model: 'test-llm_model',
    temperature: 3.14,
    machine_tier: 'starter',
    container_status: 'pending',
    openclaw_version: 'test-openclaw_version',
    tools_enabled: { key: 'value' },
    sandbox_level: 'strict',
    status: 'active',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { account_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
