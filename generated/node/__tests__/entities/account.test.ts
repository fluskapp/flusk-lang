// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AccountRepository } from '../../src/repositories/account.repository.js';

describe('AccountRepository', () => {
  let db: Database.Database;
  let repo: AccountRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE accounts (id TEXT PRIMARY KEY, type TEXT NOT NULL, name TEXT NOT NULL, slug TEXT NOT NULL, owner_email TEXT NOT NULL, plan TEXT NOT NULL, llm_mode TEXT NOT NULL, llm_budget_cents INTEGER, status TEXT NOT NULL, trial_ends_at TEXT, stripe_customer_id TEXT, stripe_subscription_id TEXT, sso_provider TEXT, sso_config TEXT, max_assistants INTEGER NOT NULL, max_users INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AccountRepository(db);
  });

  const testData = () => ({
    type: 'personal',
    name: 'test-name',
    slug: 'test-slug',
    owner_email: 'test-owner_email',
    plan: 'free',
    llm_mode: 'managed',
    status: 'active',
    max_assistants: 42,
    max_users: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { type: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
