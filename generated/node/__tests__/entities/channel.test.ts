// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { ChannelRepository } from '../../src/repositories/channel.repository.js';

describe('ChannelRepository', () => {
  let db: Database.Database;
  let repo: ChannelRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE channels (id TEXT PRIMARY KEY, assistant_id INTEGER NOT NULL, type TEXT NOT NULL, status TEXT NOT NULL, config TEXT NOT NULL, external_id TEXT, last_message_at TEXT, message_count INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new ChannelRepository(db);
  });

  const testData = () => ({
    assistant_id: 42,
    type: 'whatsapp',
    status: 'pending',
    config: { key: 'value' },
    message_count: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { assistant_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
