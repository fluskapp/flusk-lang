// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { ScheduleRepository } from '../../src/repositories/schedule.repository.js';

describe('ScheduleRepository', () => {
  let db: Database.Database;
  let repo: ScheduleRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE schedules (id TEXT PRIMARY KEY, assistant_id INTEGER NOT NULL, name TEXT NOT NULL, cron_expression TEXT, interval_ms INTEGER, prompt TEXT NOT NULL, timezone TEXT NOT NULL, last_run_at TEXT, next_run_at TEXT, run_count INTEGER NOT NULL, status TEXT NOT NULL, delivery TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new ScheduleRepository(db);
  });

  const testData = () => ({
    assistant_id: 42,
    name: 'test-name',
    prompt: 'test-prompt',
    timezone: 'test-timezone',
    run_count: 42,
    status: 'active',
    delivery: 'channel',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { assistant_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
