// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { SecretRepository } from '../../src/repositories/secret.repository.js';

describe('SecretRepository', () => {
  let db: Database.Database;
  let repo: SecretRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE secrets (id TEXT PRIMARY KEY, account_id INTEGER NOT NULL, assistant_id INTEGER, key TEXT NOT NULL, encrypted_value TEXT NOT NULL, provider TEXT, expires_at TEXT, rotated_at TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new SecretRepository(db);
  });

  const testData = () => ({
    account_id: 42,
    key: 'test-key',
    encrypted_value: 'test-encrypted_value',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { account_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
