// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { KnowledgeBaseRepository } from '../../src/repositories/knowledgebase.repository.js';

describe('KnowledgeBaseRepository', () => {
  let db: Database.Database;
  let repo: KnowledgeBaseRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE knowledge_bases (id TEXT PRIMARY KEY, solution_id INTEGER NOT NULL, name TEXT NOT NULL, type TEXT NOT NULL, source TEXT NOT NULL, content_hash TEXT, chunk_count INTEGER NOT NULL, status TEXT NOT NULL, error_message TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new KnowledgeBaseRepository(db);
  });

  const testData = () => ({
    solution_id: 42,
    name: 'test-name',
    type: 'file',
    source: 'test-source',
    chunk_count: 42,
    status: 'pending',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { solution_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
