// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { DeploymentRepository } from '../../src/repositories/deployment.repository.js';

describe('DeploymentRepository', () => {
  let db: Database.Database;
  let repo: DeploymentRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE deployments (id TEXT PRIMARY KEY, solution_id INTEGER NOT NULL, org_id INTEGER NOT NULL, channel TEXT NOT NULL, channel_config TEXT NOT NULL, status TEXT NOT NULL, instance_id TEXT NOT NULL, health_status TEXT NOT NULL, last_health_check TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new DeploymentRepository(db);
  });

  const testData = () => ({
    solution_id: 42,
    org_id: 42,
    channel: 'slack',
    channel_config: { key: 'value' },
    status: 'active',
    instance_id: 'test-instance_id',
    health_status: 'healthy',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { solution_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
