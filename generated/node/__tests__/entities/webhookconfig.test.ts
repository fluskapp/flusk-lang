// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { WebhookConfigRepository } from '../../src/repositories/webhookconfig.repository.js';

describe('WebhookConfigRepository', () => {
  let db: Database.Database;
  let repo: WebhookConfigRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE webhook_configs (id TEXT PRIMARY KEY, bot_id TEXT NOT NULL, verify_token TEXT NOT NULL, api_secret TEXT NOT NULL, provider TEXT NOT NULL, webhook_url TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new WebhookConfigRepository(db);
  });

  const testData = () => ({
    bot_id: 'test-bot_id',
    verify_token: 'test-verify_token',
    api_secret: 'test-api_secret',
    provider: 'waha',
    webhook_url: 'test-webhook_url',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { bot_id: 'updated' });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
