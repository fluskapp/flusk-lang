// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AgentConfigRepository } from '../../src/repositories/agentconfig.repository.js';

describe('AgentConfigRepository', () => {
  let db: Database.Database;
  let repo: AgentConfigRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE agent_configs (id TEXT PRIMARY KEY, org_id INTEGER NOT NULL, version INTEGER NOT NULL, otel_endpoint TEXT NOT NULL, heartbeat_interval_ms INTEGER NOT NULL, approved_tools TEXT NOT NULL, intercept_patterns TEXT NOT NULL, solutions TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AgentConfigRepository(db);
  });

  const testData = () => ({
    org_id: 42,
    version: 42,
    otel_endpoint: 'test-otel_endpoint',
    heartbeat_interval_ms: 42,
    approved_tools: { key: 'value' },
    intercept_patterns: { key: 'value' },
    solutions: { key: 'value' },
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { org_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
