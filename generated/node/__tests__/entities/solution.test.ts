// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { SolutionRepository } from '../../src/repositories/solution.repository.js';

describe('SolutionRepository', () => {
  let db: Database.Database;
  let repo: SolutionRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE solutions (id TEXT PRIMARY KEY, org_id INTEGER NOT NULL, name TEXT NOT NULL, description TEXT, type TEXT NOT NULL, llm_provider TEXT NOT NULL, llm_model TEXT NOT NULL, system_prompt TEXT, temperature REAL NOT NULL, max_tokens INTEGER NOT NULL, tools_config TEXT, status TEXT NOT NULL, created_by INTEGER NOT NULL, published_at TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new SolutionRepository(db);
  });

  const testData = () => ({
    org_id: 42,
    name: 'test-name',
    type: 'openclaw',
    llm_provider: 'openai',
    llm_model: 'test-llm_model',
    temperature: 3.14,
    max_tokens: 42,
    status: 'draft',
    created_by: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { org_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
