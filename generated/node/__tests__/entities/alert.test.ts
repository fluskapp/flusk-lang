// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { AlertRepository } from '../../src/repositories/alert.repository.js';

describe('AlertRepository', () => {
  let db: Database.Database;
  let repo: AlertRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE alerts (id TEXT PRIMARY KEY, org_id INTEGER NOT NULL, rule_id INTEGER, type TEXT NOT NULL, severity TEXT NOT NULL, title TEXT NOT NULL, message TEXT NOT NULL, context TEXT, status TEXT NOT NULL, resolved_at TEXT, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new AlertRepository(db);
  });

  const testData = () => ({
    org_id: 42,
    type: 'shadow-ai',
    severity: 'low',
    title: 'test-title',
    message: 'test-message',
    status: 'open',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { org_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
