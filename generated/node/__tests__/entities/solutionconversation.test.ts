// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { SolutionConversationRepository } from '../../src/repositories/solutionconversation.repository.js';

describe('SolutionConversationRepository', () => {
  let db: Database.Database;
  let repo: SolutionConversationRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE solution_conversations (id TEXT PRIMARY KEY, deployment_id INTEGER NOT NULL, org_id INTEGER NOT NULL, user_identifier TEXT NOT NULL, message_count INTEGER NOT NULL, input_tokens INTEGER NOT NULL, output_tokens INTEGER NOT NULL, cost_usd REAL NOT NULL, satisfaction INTEGER, last_message_at TEXT NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new SolutionConversationRepository(db);
  });

  const testData = () => ({
    deployment_id: 42,
    org_id: 42,
    user_identifier: 'test-user_identifier',
    message_count: 42,
    input_tokens: 42,
    output_tokens: 42,
    cost_usd: 3.14,
    last_message_at: '2026-01-01T00:00:00.000Z',
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { deployment_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
