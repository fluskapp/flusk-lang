// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeEach } from 'vitest';
import Database from 'better-sqlite3';
import { UsageDailyRepository } from '../../src/repositories/usagedaily.repository.js';

describe('UsageDailyRepository', () => {
  let db: Database.Database;
  let repo: UsageDailyRepository;

  beforeEach(() => {
    db = new Database(':memory:');
    db.exec(`CREATE TABLE usage_dailys (id TEXT PRIMARY KEY, org_id INTEGER NOT NULL, device_id TEXT NOT NULL, provider TEXT NOT NULL, model TEXT NOT NULL, date TEXT NOT NULL, request_count INTEGER NOT NULL, total_input_tokens INTEGER NOT NULL, total_output_tokens INTEGER NOT NULL, total_cost_usd REAL NOT NULL, total_latency_ms INTEGER NOT NULL, shadow_count INTEGER NOT NULL, created_at TEXT NOT NULL DEFAULT (datetime('now')), updated_at TEXT NOT NULL DEFAULT (datetime('now')))`);
    repo = new UsageDailyRepository(db);
  });

  const testData = () => ({
    org_id: 42,
    device_id: 'test-device_id',
    provider: 'test-provider',
    model: 'test-model',
    date: '2026-01-01T00:00:00.000Z',
    request_count: 42,
    total_input_tokens: 42,
    total_output_tokens: 42,
    total_cost_usd: 3.14,
    total_latency_ms: 42,
    shadow_count: 42,
  });

  it('creates a record', () => {
    const result = repo.create(testData());
    expect(result).toBeDefined();
    expect(result.id).toBeDefined();
  });

  it('finds by id', () => {
    const created = repo.create(testData());
    const found = repo.findById(created.id);
    expect(found).toBeDefined();
    expect(found!.id).toBe(created.id);
  });

  it('lists records', () => {
    repo.create(testData());
    repo.create(testData());
    const list = repo.list();
    expect(list.length).toBe(2);
  });

  it('updates a record', () => {
    const created = repo.create(testData());
    const updated = repo.update(created.id, { org_id: 99 });
    expect(updated).toBeDefined();
  });

  it('deletes a record', () => {
    const created = repo.create(testData());
    const deleted = repo.delete(created.id);
    expect(deleted).toBe(true);
    expect(repo.findById(created.id)).toBeNull();
  });
});
