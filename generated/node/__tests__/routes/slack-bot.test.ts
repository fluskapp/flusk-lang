// @generated by flusk-lang — DO NOT EDIT

import { describe, it, expect, beforeAll, afterAll } from 'vitest';
import Fastify from 'fastify';
import Database from 'better-sqlite3';
import { slackBotRoutes } from '../../src/routes/slack-bot.routes.js';

describe('slack-bot routes', () => {
  const app = Fastify();
  let db: Database.Database;

  beforeAll(async () => {
    db = new Database(':memory:');
    db.pragma('journal_mode = WAL');
    app.decorate('db', db);
    await app.register(slackBotRoutes);
    await app.ready();
  });

  afterAll(async () => {
    await app.close();
    db.close();
  });

  it('POST /api/slack-bot/integrations/slack/webhook', async () => {
    const res = await app.inject({
      method: 'POST',
      url: '/api/slack-bot/integrations/slack/webhook',
      payload: {},
    });
    expect(res.statusCode).toBeLessThan(500);
  });

  it('GET /api/slack-bot/api/slack/settings', async () => {
    const res = await app.inject({
      method: 'GET',
      url: '/api/slack-bot/api/slack/settings',
    });
    expect(res.statusCode).toBeLessThan(500);
  });

});
