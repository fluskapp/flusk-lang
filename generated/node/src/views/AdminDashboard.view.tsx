// @generated by flusk-lang — DO NOT EDIT

import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/$tenant/admin')({
  component: AdminDashboard,
})

function AdminDashboard() {
  const data: any = {} // TODO: wire to loader

  return (
    <div className="flex flex-col gap-6 p-6">
        <div className="grid grid-cols-3 gap-4">
          <div className="rounded-lg border bg-card p-6 shadow-sm">
            <p className="text-sm text-muted-foreground">Total Users</p>
            <p className="text-2xl font-bold">{data.stats.totalUsers}</p>
          </div>
          <div className="rounded-lg border bg-card p-6 shadow-sm">
            <p className="text-sm text-muted-foreground">Monthly Revenue</p>
            <p className="text-2xl font-bold">{'$' + data.stats.revenue?.toLocaleString()}</p>
          </div>
          <div className="rounded-lg border bg-card p-6 shadow-sm">
            <p className="text-sm text-muted-foreground">Growth</p>
            <p className="text-2xl font-bold">{data.stats.growth + '%'}</p>
          </div>
        </div>
        {/* Usage Over Time: area chart — date × count */}
        <div className="rounded-lg border bg-card p-6 shadow-sm">
          <h3 className="text-lg font-semibold mb-4">Usage Over Time</h3>
          <div className="h-64 flex items-center justify-center text-muted-foreground">
            Chart: area (date × count)
          </div>
        </div>
        <div className="rounded-lg border bg-card shadow-sm overflow-hidden">
          <h3 className="text-lg font-semibold p-6 pb-0">Solutions</h3>
          <div className="overflow-x-auto">
            <table className="w-full">
              <thead className="border-b">
                <tr>
              <th className="px-4 py-3 text-left text-sm font-medium text-muted-foreground">name</th>
              <th className="px-4 py-3 text-left text-sm font-medium text-muted-foreground">status</th>
              <th className="px-4 py-3 text-left text-sm font-medium text-muted-foreground">createdAt</th>
              <th className="px-4 py-3 text-left text-sm font-medium text-muted-foreground">usage</th>
              <th className="px-4 py-3 text-right text-sm font-medium text-muted-foreground">Actions</th>
                </tr>
              </thead>
              <tbody>
                {data.solutions?.map((row: any, i: number) => (
                  <tr key={i} className="border-b last:border-0">
                <td className="px-4 py-3 text-sm">{row.name}</td>
                <td className="px-4 py-3 text-sm">{row.status}</td>
                <td className="px-4 py-3 text-sm">{row.createdAt}</td>
                <td className="px-4 py-3 text-sm">{row.usage}</td>
                <td className="px-4 py-3 text-sm text-right flex gap-2 justify-end"><button className="text-sm text-primary hover:underline">edit</button> <button className="text-sm text-primary hover:underline">delete</button></td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>
    </div>
  )
}
