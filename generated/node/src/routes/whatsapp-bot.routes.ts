// @generated by flusk-lang — DO NOT EDIT

import { type FastifyInstance } from 'fastify';
import type Database from 'better-sqlite3';
import { webhookIncoming } from '../functions/webhookIncoming.function.js';
import { webhookVerify } from '../functions/webhookVerify.function.js';
import { createBot } from '../functions/createBot.function.js';
import { updateBot } from '../functions/updateBot.function.js';
import { listBots } from '../functions/listBots.function.js';
import { getBotStats } from '../functions/getBotStats.function.js';
import { listConversations } from '../functions/listConversations.function.js';
import { getConversationMessages } from '../functions/getConversationMessages.function.js';

interface AppWithDb extends FastifyInstance { db: Database.Database; }

export const whatsappBotRoutes = async (app: FastifyInstance): Promise<void> => {
  const { db } = app as AppWithDb;

  app.post('/api/whatsapp-bot/webhooks/whatsapp/:botId', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = webhookIncoming(db, body);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'webhookIncoming failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/whatsapp-bot/webhooks/whatsapp/:botId', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = webhookVerify(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'webhookVerify failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.post('/api/whatsapp-bot/api/bots', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = createBot(db, body);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'createBot failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.put('/api/whatsapp-bot/api/bots/:id', async (request, reply) => {
    try {
      const body = request.body as Record<string, unknown>;
      const result = updateBot(db, body);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'updateBot failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/whatsapp-bot/api/bots', async (request, reply) => {
    try {
      const result = listBots(db);
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'listBots failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/whatsapp-bot/api/bots/:id/stats', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = getBotStats(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'getBotStats failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/whatsapp-bot/api/bots/:id/conversations', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = listConversations(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'listConversations failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

  app.get('/api/whatsapp-bot/api/bots/:id/conversations/:conversationId/messages', async (request, reply) => {
    try {
      const { id } = request.params as Record<string, string>;
      const result = getConversationMessages(db, id);
      if (result === null || result === undefined) {
        return reply.status(404).send({ error: 'Not found' });
      }
      return reply.send(result);
    } catch (err: unknown) {
      request.log.error(err, 'getConversationMessages failed');
      return reply.status(500).send({ error: 'Internal server error' });
    }
  });

};
