// @generated by flusk-lang — DO NOT EDIT

import type { PlatformaticApp } from '@platformatic/db';
import type { FastifyRequest } from 'fastify';

export const publishSolution = async (
  platformatic: PlatformaticApp,
  request: FastifyRequest,
): Promise<unknown> => {
  // Validate: deployment-input
  const created = await platformatic.entities.deployment.save({
    input: request.body as Record<string, unknown>,
  });
  // Emit event via request context
  (request.server as any).events?.emit('solution-published', request.body);
  return { ok: true };
};
