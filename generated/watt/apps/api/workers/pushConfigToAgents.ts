// @generated by flusk-lang — DO NOT EDIT

import type { PlatformaticApp } from '@platformatic/db';

export const pushConfigToAgents = async (
  platformatic: PlatformaticApp,
  payload: Record<string, unknown>,
): Promise<void> => {
  const agentConfig = await platformatic.entities.agentConfig.find({
    where: { id: { eq: payload.agentConfig_id ?? payload.id } },
    limit: 1,
  }).then((r: unknown[]) => r[0]);
  const agentDevice = await platformatic.entities.agentDevice.find({
    where: { id: { eq: payload.agentDevice_id ?? payload.id } },
    limit: 1,
  }).then((r: unknown[]) => r[0]);
  // Call: push-to-agents
  // TODO: wire to function
};
