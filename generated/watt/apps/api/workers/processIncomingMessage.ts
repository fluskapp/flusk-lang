// @generated by flusk-lang — DO NOT EDIT

import type { PlatformaticApp } from '@platformatic/db';

export const processIncomingMessage = async (
  platformatic: PlatformaticApp,
  payload: Record<string, unknown>,
): Promise<void> => {
  const message = await platformatic.entities.message.find({
    where: { id: { eq: payload.message_id ?? payload.id } },
    limit: 1,
  }).then((r: unknown[]) => r[0]);
  // Call: generate-ai-reply
  // TODO: wire to function
  // Call: send-whatsapp-message
  // TODO: wire to function
  await platformatic.entities.conversation.save({
    input: { ...payload },
  });
};
