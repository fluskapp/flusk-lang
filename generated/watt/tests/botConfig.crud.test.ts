// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "workspace_id": "test-workspace_id",
  "bot_token": "test-bot_token",
  "active": true
};

test('BotConfig CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /bot_configs', async () => {
    const res = await fetch(`${baseUrl}/bot_configs`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /bot_configs', async () => {
    const res = await fetch(`${baseUrl}/bot_configs`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /bot_configs/1', async () => {
    const res = await fetch(`${baseUrl}/bot_configs/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ workspace_id: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /bot_configs/1', async () => {
    const res = await fetch(`${baseUrl}/bot_configs/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
