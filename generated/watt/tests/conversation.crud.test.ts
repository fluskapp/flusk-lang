// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "bot_id": "test-bot_id",
  "contact_phone": "test-contact_phone",
  "contact_name": "test-contact_name",
  "status": "active",
  "last_message_at": "test"
};

test('Conversation CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /conversations', async () => {
    const res = await fetch(`${baseUrl}/conversations`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /conversations', async () => {
    const res = await fetch(`${baseUrl}/conversations`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /conversations/1', async () => {
    const res = await fetch(`${baseUrl}/conversations/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ bot_id: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /conversations/1', async () => {
    const res = await fetch(`${baseUrl}/conversations/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
