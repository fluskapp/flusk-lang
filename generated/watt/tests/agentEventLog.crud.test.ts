// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "device_id": "test-device_id",
  "event_type": "connected",
  "details": "test"
};

test('AgentEventLog CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /agent_event_logs', async () => {
    const res = await fetch(`${baseUrl}/agent_event_logs`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /agent_event_logs', async () => {
    const res = await fetch(`${baseUrl}/agent_event_logs`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /agent_event_logs/1', async () => {
    const res = await fetch(`${baseUrl}/agent_event_logs/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ device_id: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /agent_event_logs/1', async () => {
    const res = await fetch(`${baseUrl}/agent_event_logs/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
