// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "name": "test-name",
  "provider": "test-provider",
  "category": "chat",
  "approved": true,
  "detection_pattern": "test-detection_pattern",
  "first_seen_at": "test",
  "last_seen_at": "test"
};

test('AiTool CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /ai_tools', async () => {
    const res = await fetch(`${baseUrl}/ai_tools`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /ai_tools', async () => {
    const res = await fetch(`${baseUrl}/ai_tools`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /ai_tools/1', async () => {
    const res = await fetch(`${baseUrl}/ai_tools/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /ai_tools/1', async () => {
    const res = await fetch(`${baseUrl}/ai_tools/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
