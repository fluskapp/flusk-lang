// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "deployment_id": "test",
  "org_id": "test",
  "user_identifier": "test-user_identifier",
  "message_count": "test",
  "input_tokens": "test",
  "output_tokens": "test",
  "cost_usd": 1,
  "satisfaction": "test",
  "last_message_at": "test"
};

test('SolutionConversation CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /solution_conversations', async () => {
    const res = await fetch(`${baseUrl}/solution_conversations`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /solution_conversations', async () => {
    const res = await fetch(`${baseUrl}/solution_conversations`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /solution_conversations/1', async () => {
    const res = await fetch(`${baseUrl}/solution_conversations/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ user_identifier: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /solution_conversations/1', async () => {
    const res = await fetch(`${baseUrl}/solution_conversations/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
