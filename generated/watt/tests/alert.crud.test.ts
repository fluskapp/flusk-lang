// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "rule_id": "test",
  "type": "shadow-ai",
  "severity": "low",
  "title": "test-title",
  "message": "test-message",
  "context": "test",
  "status": "open",
  "resolved_at": "test"
};

test('Alert CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /alerts', async () => {
    const res = await fetch(`${baseUrl}/alerts`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /alerts', async () => {
    const res = await fetch(`${baseUrl}/alerts`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /alerts/1', async () => {
    const res = await fetch(`${baseUrl}/alerts/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ title: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /alerts/1', async () => {
    const res = await fetch(`${baseUrl}/alerts/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
