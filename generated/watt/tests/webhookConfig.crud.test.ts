// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "bot_id": "test-bot_id",
  "verify_token": "test-verify_token",
  "api_secret": "test-api_secret",
  "provider": "waha",
  "webhook_url": "test-webhook_url"
};

test('WebhookConfig CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /webhook_configs', async () => {
    const res = await fetch(`${baseUrl}/webhook_configs`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /webhook_configs', async () => {
    const res = await fetch(`${baseUrl}/webhook_configs`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /webhook_configs/1', async () => {
    const res = await fetch(`${baseUrl}/webhook_configs/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ bot_id: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /webhook_configs/1', async () => {
    const res = await fetch(`${baseUrl}/webhook_configs/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
