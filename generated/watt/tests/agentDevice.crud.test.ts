// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "device_id": "test-device_id",
  "hostname": "test-hostname",
  "os": "test-os",
  "arch": "test-arch",
  "agent_version": "test-agent_version",
  "employee_email": "test-employee_email",
  "last_seen_at": "test",
  "status": "active"
};

test('AgentDevice CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /agent_devices', async () => {
    const res = await fetch(`${baseUrl}/agent_devices`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /agent_devices', async () => {
    const res = await fetch(`${baseUrl}/agent_devices`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /agent_devices/1', async () => {
    const res = await fetch(`${baseUrl}/agent_devices/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ device_id: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /agent_devices/1', async () => {
    const res = await fetch(`${baseUrl}/agent_devices/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
