// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "name": "test-name",
  "description": "test-description",
  "type": "openclaw",
  "llm_provider": "openai",
  "llm_model": "test-llm_model",
  "system_prompt": "test-system_prompt",
  "temperature": 1,
  "max_tokens": "test",
  "tools_config": "test",
  "status": "draft",
  "created_by": "test",
  "published_at": "test"
};

test('Solution CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /solutions', async () => {
    const res = await fetch(`${baseUrl}/solutions`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /solutions', async () => {
    const res = await fetch(`${baseUrl}/solutions`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /solutions/1', async () => {
    const res = await fetch(`${baseUrl}/solutions/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /solutions/1', async () => {
    const res = await fetch(`${baseUrl}/solutions/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
