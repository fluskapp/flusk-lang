// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "version": "test",
  "otel_endpoint": "test-otel_endpoint",
  "heartbeat_interval_ms": "test",
  "approved_tools": "test",
  "intercept_patterns": "test",
  "solutions": "test"
};

test('AgentConfig CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /agent_configs', async () => {
    const res = await fetch(`${baseUrl}/agent_configs`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /agent_configs', async () => {
    const res = await fetch(`${baseUrl}/agent_configs`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /agent_configs/1', async () => {
    const res = await fetch(`${baseUrl}/agent_configs/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ otel_endpoint: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /agent_configs/1', async () => {
    const res = await fetch(`${baseUrl}/agent_configs/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
