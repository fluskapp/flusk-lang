// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "device_id": "test-device_id",
  "provider": "test-provider",
  "model": "test-model",
  "date": "test",
  "request_count": "test",
  "total_input_tokens": "test",
  "total_output_tokens": "test",
  "total_cost_usd": 1,
  "total_latency_ms": "test",
  "shadow_count": "test"
};

test('UsageDaily CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /usage_dailys', async () => {
    const res = await fetch(`${baseUrl}/usage_dailys`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /usage_dailys', async () => {
    const res = await fetch(`${baseUrl}/usage_dailys`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /usage_dailys/1', async () => {
    const res = await fetch(`${baseUrl}/usage_dailys/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ device_id: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /usage_dailys/1', async () => {
    const res = await fetch(`${baseUrl}/usage_dailys/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
