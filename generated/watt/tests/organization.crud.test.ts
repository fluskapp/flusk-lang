// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "name": "test-name",
  "slug": "test-slug",
  "plan": "starter",
  "max_seats": "test",
  "api_key": "test-api_key",
  "status": "active",
  "trial_ends_at": "test"
};

test('Organization CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /organizations', async () => {
    const res = await fetch(`${baseUrl}/organizations`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /organizations', async () => {
    const res = await fetch(`${baseUrl}/organizations`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /organizations/1', async () => {
    const res = await fetch(`${baseUrl}/organizations/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /organizations/1', async () => {
    const res = await fetch(`${baseUrl}/organizations/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
