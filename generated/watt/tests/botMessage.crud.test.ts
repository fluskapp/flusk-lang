// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "slack_ts": "test-slack_ts",
  "channel": "test-channel",
  "text": "test-text",
  "status": "pending"
};

test('BotMessage CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /bot_messages', async () => {
    const res = await fetch(`${baseUrl}/bot_messages`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /bot_messages', async () => {
    const res = await fetch(`${baseUrl}/bot_messages`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /bot_messages/1', async () => {
    const res = await fetch(`${baseUrl}/bot_messages/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ slack_ts: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /bot_messages/1', async () => {
    const res = await fetch(`${baseUrl}/bot_messages/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
