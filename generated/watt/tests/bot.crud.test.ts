// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "name": "test-name",
  "phone_number": "test-phone_number",
  "system_prompt": "test-system_prompt",
  "model": "gpt-4o",
  "temperature": 1,
  "max_tokens": 1,
  "active": true,
  "owner_id": "test-owner_id"
};

test('Bot CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /bots', async () => {
    const res = await fetch(`${baseUrl}/bots`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /bots', async () => {
    const res = await fetch(`${baseUrl}/bots`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /bots/1', async () => {
    const res = await fetch(`${baseUrl}/bots/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /bots/1', async () => {
    const res = await fetch(`${baseUrl}/bots/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
