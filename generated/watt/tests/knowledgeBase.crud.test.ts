// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "solution_id": "test",
  "name": "test-name",
  "type": "file",
  "source": "test-source",
  "content_hash": "test-content_hash",
  "chunk_count": "test",
  "status": "pending",
  "error_message": "test-error_message"
};

test('KnowledgeBase CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /knowledge_bases', async () => {
    const res = await fetch(`${baseUrl}/knowledge_bases`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /knowledge_bases', async () => {
    const res = await fetch(`${baseUrl}/knowledge_bases`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /knowledge_bases/1', async () => {
    const res = await fetch(`${baseUrl}/knowledge_bases/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /knowledge_bases/1', async () => {
    const res = await fetch(`${baseUrl}/knowledge_bases/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
