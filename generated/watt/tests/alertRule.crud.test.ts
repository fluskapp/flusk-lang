// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "org_id": "test",
  "name": "test-name",
  "type": "shadow-ai",
  "threshold": "test",
  "channels": "test",
  "enabled": true
};

test('AlertRule CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /alert_rules', async () => {
    const res = await fetch(`${baseUrl}/alert_rules`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /alert_rules', async () => {
    const res = await fetch(`${baseUrl}/alert_rules`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /alert_rules/1', async () => {
    const res = await fetch(`${baseUrl}/alert_rules/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /alert_rules/1', async () => {
    const res = await fetch(`${baseUrl}/alert_rules/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
