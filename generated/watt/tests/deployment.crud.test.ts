// @generated by flusk-lang — DO NOT EDIT

import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildServer } from '@platformatic/db';

const fixture = {
  "solution_id": "test",
  "org_id": "test",
  "channel": "slack",
  "channel_config": "test",
  "status": "active",
  "instance_id": "test-instance_id",
  "health_status": "healthy",
  "last_health_check": "test"
};

test('Deployment CRUD', async (t) => {
  const app = await buildServer('./apps/db/platformatic.json');
  await app.start();
  const baseUrl = app.url;

  await t.test('POST /deployments', async () => {
    const res = await fetch(`${baseUrl}/deployments`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(fixture),
    });
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(body.id);
  });

  await t.test('GET /deployments', async () => {
    const res = await fetch(`${baseUrl}/deployments`);
    assert.equal(res.status, 200);
    const body = await res.json();
    assert.ok(Array.isArray(body));
    assert.ok(body.length > 0);
  });

  await t.test('PUT /deployments/1', async () => {
    const res = await fetch(`${baseUrl}/deployments/1`, {
      method: 'PUT',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ instance_id: 'updated' }),
    });
    assert.equal(res.status, 200);
  });

  await t.test('DELETE /deployments/1', async () => {
    const res = await fetch(`${baseUrl}/deployments/1`, { method: 'DELETE' });
    assert.equal(res.status, 200);
  });

  await app.close();
});
