// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useContacts } from '../hooks/useContacts';
import { Pencil, Plus, Search, Trash2, X } from 'lucide-react';
import { Badge } from '../../components/ui/badge';
import { Button } from '../../components/ui/button';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';
import { Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter, DialogClose } from '../../components/ui/dialog';
import { Input } from '../../components/ui/input';
import { Label } from '../../components/ui/label';
import { toast } from 'sonner';
import { Table, TableHeader, TableBody, TableRow, TableHead, TableCell } from '../../components/ui/table';


function CrudTableSection({ title, columns, data }: { title?: string; columns: Array<{ key: string; label: string; [key: string]: any }>; data?: any[] }) {
  const [items, setItems] = React.useState<any[]>(data && data.length > 0 ? data : []);
  const [dialogOpen, setDialogOpen] = React.useState(false);
  const [dialogMode, setDialogMode] = React.useState<'create' | 'edit'>('create');
  const [editItem, setEditItem] = React.useState<any>(null);
  const [form, setForm] = React.useState<Record<string, string>>({});
  const visibleCols = columns.filter((c) => c.label);
  const colDefs = visibleCols.length > 0 ? visibleCols : Object.keys(items[0] ?? {}).filter((k) => k !== 'id').slice(0, 5).map((k) => ({ key: k, label: k.charAt(0).toUpperCase() + k.slice(1) }));
  const openCreate = () => { setForm({}); setDialogMode('create'); setEditItem(null); setDialogOpen(true); };
  const openEdit = (item: any) => { setForm({ ...item }); setDialogMode('edit'); setEditItem(item); setDialogOpen(true); };
  const handleSave = () => { if (dialogMode === 'create') { setItems((prev) => [...prev, { id: Date.now(), ...form }]); toast.success('Created successfully'); } else { setItems((prev) => prev.map((r) => (r.id === editItem?.id ? { ...r, ...form } : r))); toast.success('Saved successfully'); } setDialogOpen(false); };
  const handleDelete = (item: any) => { setItems((prev) => prev.filter((r) => r.id !== item.id)); toast('Deleted'); };
  return (
    <Card>
      <CardHeader className="flex flex-row items-center justify-between">
        {title && <CardTitle className="text-sm">{title}</CardTitle>}
        <Button size="sm" onClick={openCreate} style={{ backgroundColor: '#18181b' }}><Plus className="w-3.5 h-3.5 mr-1" /> New</Button>
      </CardHeader>
      <CardContent className="p-0">
        <Table><TableHeader><TableRow>{colDefs.map((col) => <TableHead key={col.key}>{col.label}</TableHead>)}<TableHead className="text-right">Actions</TableHead></TableRow></TableHeader>
          <TableBody>{items.map((row: any, i: number) => (<TableRow key={row.id ?? i}>{colDefs.map((col) => { const val = row[col.key]; const isStatus = col.key === 'status'; if (isStatus) { const v = String(val ?? '').toLowerCase(); const variant: 'default' | 'secondary' | 'outline' = v === 'active' ? 'default' : v === 'paused' ? 'secondary' : 'outline'; return <TableCell key={col.key}><Badge variant={variant}>{val}</Badge></TableCell>; } return <TableCell key={col.key}>{val != null ? String(val) : <span className="text-black/20">—</span>}</TableCell>; })}<TableCell className="text-right"><div className="flex items-center justify-end gap-1"><Button variant="ghost" size="sm" onClick={() => openEdit(row)}><Pencil className="w-3.5 h-3.5" /></Button><Button variant="ghost" size="sm" onClick={() => handleDelete(row)}><Trash2 className="w-3.5 h-3.5 text-red-500" /></Button></div></TableCell></TableRow>))}</TableBody>
        </Table>
      </CardContent>
      <Dialog open={dialogOpen} onOpenChange={setDialogOpen}>
        <DialogContent><DialogHeader><DialogTitle>{dialogMode === 'create' ? 'Create New Item' : 'Edit Item'}</DialogTitle><DialogDescription>{dialogMode === 'create' ? 'Fill in the details below.' : 'Update the fields below.'}</DialogDescription></DialogHeader>
          <div className="space-y-4 py-4">{colDefs.filter((c) => c.key !== 'id').map((col) => (<div key={col.key} className="space-y-2"><Label>{col.label}</Label><Input value={form[col.key] ?? ''} onChange={(e: React.ChangeEvent<HTMLInputElement>) => setForm((f) => ({ ...f, [col.key]: e.target.value }))} placeholder={col.label} /></div>))}</div>
          <DialogFooter><DialogClose><Button variant="ghost">Cancel</Button></DialogClose><Button onClick={handleSave} style={{ backgroundColor: '#18181b' }}>{dialogMode === 'create' ? 'Create' : 'Save Changes'}</Button></DialogFooter>
        </DialogContent>
      </Dialog>
    </Card>
  );
}

export function ContactsPage() {
  const { data = {} as any, isLoading } = useContacts();
  const navigate = (to: string) => { window.location.href = to; };
  const [activeStatus, setActiveStatus] = React.useState<string>('');

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
          <div>
            <h1 className="text-2xl font-bold tracking-tight">Contacts</h1>
            <p className="mt-1 text-sm text-muted-foreground">Who can talk to your bot, and what they can do</p>
          </div>
          <div className="flex flex-wrap gap-2">
            <Button variant="ghost" size="sm" onClick={() => { navigate('modal'); }}>Add contact</Button>
          </div>
        </div>
        <h3 className="text-sm font-semibold text-black/70 mb-3">stats</h3>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
          <Card>
            <CardContent className="p-5">
              <div className="flex items-start justify-between">
                <p className="text-sm font-medium text-black/50 leading-tight">Total</p>
              </div>
              <p className="mt-3 text-2xl font-semibold text-black tabular-nums">{data?.contacts ?? '—'}</p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <div className="flex items-start justify-between">
                <p className="text-sm font-medium text-black/50 leading-tight">Trusted</p>
              </div>
              <p className="mt-3 text-2xl font-semibold text-black tabular-nums">{data?.contacts ?? '—'}</p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <div className="flex items-start justify-between">
                <p className="text-sm font-medium text-black/50 leading-tight">Allowed</p>
              </div>
              <p className="mt-3 text-2xl font-semibold text-black tabular-nums">{data?.contacts ?? '—'}</p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <div className="flex items-start justify-between">
                <p className="text-sm font-medium text-black/50 leading-tight">Blocked</p>
              </div>
              <p className="mt-3 text-2xl font-semibold text-black tabular-nums">{data?.contacts ?? '—'}</p>
            </CardContent>
          </Card>
        </div>
        <div className="flex flex-wrap items-center gap-3 p-4 bg-white rounded-xl border border-black/10 shadow-sm">
          <div className="flex items-center gap-2 flex-1 min-w-48 px-3 py-2 border border-black/10 rounded-lg">
            <Search className="w-4 h-4 text-black/30 flex-shrink-0" />
            <input type="search" placeholder="Search..." className="flex-1 text-sm focus:outline-none placeholder-black/30" />
          </div>
          <div className="flex gap-1">
            <button onClick={() => setActiveStatus('')} className={`px-3 py-1.5 rounded-full text-xs font-medium transition-colors ${activeStatus === '' ? 'text-white' : 'bg-black/[0.02] text-black/60 hover:bg-black/[0.07]'}`} style={activeStatus === '' ? { backgroundColor: '#18181b' } : {}}>All</button>
            <button onClick={() => setActiveStatus('Trusted')} className={`px-3 py-1.5 rounded-full text-xs font-medium transition-colors ${activeStatus === 'Trusted' ? 'text-white' : 'bg-black/[0.02] text-black/60 hover:bg-black/[0.07]'}`} style={activeStatus === 'Trusted' ? { backgroundColor: '#18181b' } : {}}>Trusted</button>
            <button onClick={() => setActiveStatus('Allowed')} className={`px-3 py-1.5 rounded-full text-xs font-medium transition-colors ${activeStatus === 'Allowed' ? 'text-white' : 'bg-black/[0.02] text-black/60 hover:bg-black/[0.07]'}`} style={activeStatus === 'Allowed' ? { backgroundColor: '#18181b' } : {}}>Allowed</button>
            <button onClick={() => setActiveStatus('Blocked')} className={`px-3 py-1.5 rounded-full text-xs font-medium transition-colors ${activeStatus === 'Blocked' ? 'text-white' : 'bg-black/[0.02] text-black/60 hover:bg-black/[0.07]'}`} style={activeStatus === 'Blocked' ? { backgroundColor: '#18181b' } : {}}>Blocked</button>
          </div>
        </div>
        <CrudTableSection title="contact-list" columns={[{"key":"name","label":"Name","field":"name","format":"masked"},{"key":"identifier","label":"Identifier","field":"identifier","format":"masked"},{"key":"channel","label":"Channel","field":"channel"},{"key":"role","label":"Access","field":"role","type":"badge"},{"key":"description","label":"Context","field":"description"}]} data={(data?.contacts) as any[] | undefined} />
      </div>
    </div>
  );
}

export default ContactsPage;
