// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useOnboarding } from '../hooks/useOnboarding';
import { Badge } from '../../components/ui/badge';
import { Button } from '../../components/ui/button';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';
import { Input } from '../../components/ui/input';
import { Label } from '../../components/ui/label';
import { Separator } from '../../components/ui/separator';
import { toast } from 'sonner';

export function OnboardingPage() {
  const { data = {} as any, isLoading } = useOnboarding();
  const navigate = (to: string) => { window.location.href = to; };
  void isLoading;

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
        <div>
          <h3 className="text-sm font-semibold text-stone-700 mb-3">Pick your machine</h3>
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
            {((undefined) as any[] ?? []).map((item: any, i: number) => (
              <Card key={i} className="hover:shadow-md transition-shadow">
                <CardContent className="p-5">
                  <div className="flex items-start justify-between">
                    <h4 className="font-medium text-stone-900 text-sm">{item.name ?? item.title ?? `Item ${i + 1}`}</h4>
                    {item.status && (
                      <Badge variant={(() => { const s = (item.status ?? '').toLowerCase(); if (s === 'active' || s === 'connected') return 'default'; if (s === 'paused' || s === 'warning' || s === 'info') return 'secondary'; if (s === 'error' || s === 'critical') return 'destructive'; return 'outline'; })()}>{item.status}</Badge>
                    )}
                  </div>
                  {item.description && <p className="mt-1.5 text-xs text-stone-500 line-clamp-2">{item.description}</p>}
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
        <div className="min-h-screen flex items-center justify-center bg-stone-50 px-4">
          <div className="w-full max-w-sm">
            <div className="text-center mb-8">
              <div className="w-12 h-12 rounded-2xl mx-auto mb-5 flex items-center justify-center shadow-lg" style={{ backgroundColor: '#18181b' }}><span className="text-white font-bold text-lg">F</span></div>
              <h1 className="text-2xl font-bold text-stone-900">Sign in to Flusk</h1>
              <p className="mt-2 text-sm text-stone-500">Enter your credentials to continue</p>
            </div>
            <Card>
              <CardContent className="pt-6">
                <form onSubmit={(e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); localStorage.setItem('flusk_user', JSON.stringify({ email: fd.get('email'), loggedIn: true })); toast.success('Signed in successfully'); window.location.href = '/app/dashboard'; }} className="space-y-4">
                  <div className="space-y-2">
                    <Label htmlFor="llm_mode">llm_mode</Label>
                    <Input id="llm_mode" name="llm_mode" type="radio" placeholder=""   />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="llm_provider">Provider</Label>
                    <Input id="llm_provider" name="llm_provider" type="select" placeholder=""   />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="api_key">API Key</Label>
                    <Input id="api_key" name="api_key" type="password" placeholder="sk-..."   />
                  </div>
                </form>
              </CardContent>
            </Card>
          </div>
        </div>
        <div>
          <h3 className="text-sm font-semibold text-stone-700 mb-3">Connect your channels</h3>
          <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
            {((undefined) as any[] ?? []).map((item: any, i: number) => (
              <Card key={i} className="hover:shadow-md transition-shadow">
                <CardContent className="p-5">
                  <div className="flex items-start justify-between">
                    <h4 className="font-medium text-stone-900 text-sm">{item.name ?? item.title ?? `Item ${i + 1}`}</h4>
                    {item.status && (
                      <Badge variant={(() => { const s = (item.status ?? '').toLowerCase(); if (s === 'active' || s === 'connected') return 'default'; if (s === 'paused' || s === 'warning' || s === 'info') return 'secondary'; if (s === 'error' || s === 'critical') return 'destructive'; return 'outline'; })()}>{item.status}</Badge>
                    )}
                  </div>
                  {item.description && <p className="mt-1.5 text-xs text-stone-500 line-clamp-2">{item.description}</p>}
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}

export default OnboardingPage;
