// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useTeamAnalytics } from '../hooks/useTeamAnalytics';
import { MoreHorizontal } from 'lucide-react';
import { StatCard, Chart, DataTable } from '../components';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function TeamAnalyticsPage() {
  const { data = {} as any, isLoading } = useTeamAnalytics();
  const navigate = (to: string) => { window.location.href = to; };
  void isLoading;

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Active Users"
                value={data?.teams?.totalUsers}
                format="number"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Teams"
                value={data?.teams?.totalTeams}
                format="number"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Avg Queries / User"
                value={data?.teams?.avgQueriesPerUser}
                format="number"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="AI Adoption Rate"
                value={data?.teams?.adoptionRate}
                format="percent"
              />
            </CardContent>
          </Card>
        </div>
        <Chart
          type="line"
          data={data?.teams?.usageByTeamByDay}
          title="Usage by Team Over Time"
          xAxis="date"
          yAxis="queries"
        />
        <Chart
          type="bar"
          data={data?.teams?.byDepartment}
          title="Usage by Department"
          xAxis="department"
          yAxis="queries"
        />
        <DataTable
          title="Teams Breakdown"
          columns={["team","members","totalQueries","totalCost","adoptionRate","topSolution"]}
          data={data?.teams?.list as any[] | undefined}
          actions={["view","manage"]}
        />
        <DataTable
          title="Top Users"
          columns={["user","team","queries","cost","lastActive"]}
          data={data?.teams?.topUsers as any[] | undefined}
          actions={["view"]}
        />
      </div>
    </div>
  );
}

export default TeamAnalyticsPage;
