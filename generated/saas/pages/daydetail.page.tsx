// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useDayDetail } from '../hooks/useDayDetail';
import { CheckCircle, ChevronDown, ChevronRight, Circle, Target } from 'lucide-react';
import { Badge } from '../../components/ui/badge';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function DayDetailPage() {
  const { data = {} as any, isLoading } = useDayDetail();
  const navigate = (to: string) => { window.location.href = to; };
  const [RawEvents, setRawEvents] = React.useState(false);

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
          <div>
            <h1 className="text-2xl font-bold tracking-tight">DayHeader</h1>
          </div>
        </div>
        <Card>
          <CardHeader>
            <CardTitle className="text-sm">What Happened</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-stone-700 whitespace-pre-wrap leading-relaxed">{data?.summary?.summary_text ?? ""}</p>
          </CardContent>
        </Card>
        <h3 className="text-sm font-semibold text-stone-700 mb-3">Key Decisions</h3>
        <div className="flex flex-wrap gap-2">
          {((data?.summary?.key_decisions) as string[] ?? []).map((tag: string, i: number) => (
            <Badge key={i} variant="secondary"><CheckCircle className="w-3 h-3 mr-1" />{tag}</Badge>
          ))}
        </div>
        <h3 className="text-sm font-semibold text-stone-700 mb-3">Goals</h3>
        <div className="space-y-2">
          {((data?.summary) as any[] ?? []).map((item: any, i: number) => (
            <div key={i} className="flex items-center gap-3 py-1.5">
              {item.goals_progress != null ? <CheckCircle className="w-4 h-4 text-emerald-500 flex-shrink-0" /> : <Circle className="w-4 h-4 text-stone-300 flex-shrink-0" />}
              <span className={`text-sm ${item.goals_progress != null ? 'text-stone-500 line-through' : 'text-stone-700'}`}>{item.goals_progress}</span>
            </div>
          ))}
        </div>
        <h3 className="text-sm font-semibold text-stone-700 mb-3">Remembered</h3>
        <div className="flex flex-wrap gap-2">
          {((data?.summary?.things_to_remember) as string[] ?? []).map((tag: string, i: number) => (
            <Badge key={i} variant="secondary"><Brain className="w-3 h-3 mr-1" />{tag}</Badge>
          ))}
        </div>
        <h3 className="text-sm font-semibold text-stone-700 mb-3">Guard Report</h3>
        <div className="grid grid-cols-2 md:grid-cols-3 gap-4">
          <Card>
            <CardContent className="p-5">
              <div className="flex items-start justify-between">
                <p className="text-sm font-medium text-stone-500 leading-tight">PII Detected</p>
                <EyeOff className="w-4 h-4 text-stone-400" />
              </div>
              <p className="mt-3 text-2xl font-semibold text-stone-900 tabular-nums">{data?.summary?.pii_detections ?? '—'}</p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <div className="flex items-start justify-between">
                <p className="text-sm font-medium text-stone-500 leading-tight">Blocked Actions</p>
                <ShieldOff className="w-4 h-4 text-stone-400" />
              </div>
              <p className="mt-3 text-2xl font-semibold text-stone-900 tabular-nums">{data?.summary?.guard_blocks ?? '—'}</p>
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <div className="flex items-start justify-between">
                <p className="text-sm font-medium text-stone-500 leading-tight">Total Cost</p>
                <DollarSign className="w-4 h-4 text-stone-400" />
              </div>
              <p className="mt-3 text-2xl font-semibold text-stone-900 tabular-nums">{data?.summary?.cost_cents ?? '—'}</p>
            </CardContent>
          </Card>
        </div>
        <Card>
          <button onClick={() => setRawEvents((v) => !v)} className="w-full flex items-center justify-between px-5 py-4 text-left">
            <span className="text-sm font-semibold text-stone-900">Raw Events (for nerds)</span>
            {RawEvents ? <ChevronDown className="w-4 h-4 text-stone-400" /> : <ChevronRight className="w-4 h-4 text-stone-400" />}
          </button>
          {RawEvents && (
            <div className="px-5 pb-4">
              <p className="text-sm text-stone-700 whitespace-pre-wrap leading-relaxed">{data?.events?.content ?? ''}</p>
            </div>
          )}
        </Card>
      </div>
    </div>
  );
}

export default DayDetailPage;
