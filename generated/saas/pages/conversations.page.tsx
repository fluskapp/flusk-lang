// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useConversations } from '../hooks/useConversations';
import { Badge } from '../../components/ui/badge';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function ConversationsPage() {
  const { data = {} as any, isLoading } = useConversations();
  const navigate = (to: string) => { window.location.href = to; };
  void isLoading;

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <h3 className="text-sm font-semibold text-stone-700 mb-3">ConversationList</h3>
        {(() => {
          const items = (data?.conversations) as any[] ?? [];
          if (items.length === 0) return <p className="text-sm text-stone-400 py-6 text-center">No items found</p>;
          const labels: Record<string, string> = {};
          const groups: Record<string, any[]> = {};
          for (const item of items) { const key = item.date ?? 'other'; if (!groups[key]) groups[key] = []; groups[key].push(item); }
          return (<div className="space-y-4">{Object.entries(groups).map(([key, groupItems]) => (
            <div key={key}>
              <h4 className="text-xs font-semibold text-stone-500 uppercase tracking-wide mb-2">{labels[key] ?? key}</h4>
              <div className="space-y-2">
                {groupItems.map((item: any, i: number) => (
                  <Card key={i}>
                    <CardContent className="flex items-center justify-between px-4 py-3">
                      <div className="min-w-0 flex-1">
                        <p className="text-sm font-medium text-stone-900 truncate">{item.name ?? item.name}</p>
                      </div>
                    </CardContent>
                  </Card>
                ))}
              </div>
            </div>
          ))}</div>);
        })()}
      </div>
    </div>
  );
}

export default ConversationsPage;
