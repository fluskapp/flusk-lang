// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useConnectors } from '../hooks/useConnectors';
import { MoreHorizontal } from 'lucide-react';
import { DataTable } from '../components';
import { Badge } from '../../components/ui/badge';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function ConnectorsPage() {
  const { data = {} as any, isLoading } = useConnectors();
  const navigate = (to: string) => { window.location.href = to; };
  void isLoading;

  return (
    <div className="min-h-screen bg-white">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <DataTable
          title="Active Connectors"
          columns={[{"key":"name","label":"Connector","type":"text","sortable":true},{"key":"type","label":"Type","type":"status-badge"},{"key":"status","label":"Status","type":"status-badge"},{"key":"last_event","label":"Last Event","type":"relative-time"},{"key":"events_count","label":"Events","type":"number"}]}
          data={data?.connectors as any[] | undefined}
          actions={[]}
        />
        <div>
          <h3 className="text-sm font-semibold text-black/70 mb-3">Available Connectors</h3>
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
            {([{"id":"location","name":"Location","description":"Trigger actions based on where you are","category":"trigger"},{"id":"jira","name":"Jira","description":"React to ticket updates, assignments, and comments","category":"trigger"},{"id":"github-events","name":"GitHub Events","description":"PRs, issues, CI runs — your bot stays in the loop","category":"trigger"},{"id":"calendar","name":"Google Calendar","description":"Meeting prep, schedule changes, reminders","category":"trigger"},{"id":"email","name":"Email (Gmail)","description":"Incoming emails trigger bot analysis and drafts","category":"trigger"},{"id":"slack-events","name":"Slack Events","description":"Channel messages, mentions, reactions","category":"trigger"},{"id":"webhook","name":"Custom Webhook","description":"Send any JSON payload to trigger your bot","category":"trigger"},{"id":"cron","name":"Scheduled Tasks","description":"Run actions on a schedule — daily summaries, weekly reports","category":"automation"}]).map((item: any, i: number) => (
              <Card key={i} className="hover:shadow-md transition-shadow">
                <CardContent className="p-5">
                  <div className="flex items-start justify-between">
                    <h4 className="font-medium text-black text-sm">{item.name ?? item.title ?? `Item ${i + 1}`}</h4>
                    {item.status && (
                      <Badge variant={(() => { const s = (item.status ?? '').toLowerCase(); if (s === 'active' || s === 'connected') return 'default'; if (s === 'paused' || s === 'warning' || s === 'info') return 'secondary'; if (s === 'error' || s === 'critical') return 'destructive'; return 'outline'; })()}>{item.status}</Badge>
                    )}
                  </div>
                  {item.description && <p className="mt-1.5 text-xs text-black/50 line-clamp-2">{item.description}</p>}
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}

export default ConnectorsPage;
