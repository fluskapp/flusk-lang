// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useAnalytics } from '../hooks/useAnalytics';
import { MoreHorizontal } from 'lucide-react';
import { StatCard, Chart, DataTable } from '../components';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function AnalyticsPage() {
  const { data = {} as any, isLoading } = useAnalytics();
  const navigate = (to: string) => { window.location.href = to; };
  void isLoading;

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Total Queries"
                value={data?.metrics?.totalQueries}
                format="number"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Avg Latency (ms)"
                value={data?.metrics?.avgLatency}
                format="number"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Success Rate"
                value={data?.metrics?.successRate}
                format="percent"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Total Cost"
                value={data?.metrics?.costTotal}
                format="currency"
              />
            </CardContent>
          </Card>
        </div>
        <Chart
          type="line"
          data={data?.analytics?.queriesByDay}
          title="Queries Over Time"
          xAxis="date"
          yAxis="queries"
        />
        <Chart
          type="donut"
          data={data?.analytics?.costByModel}
          title="Cost Breakdown"
          xAxis="model"
          yAxis="cost"
        />
        <DataTable
          title="Top Solutions"
          columns={["name","queries","avgLatency","cost"]}
          data={data?.analytics?.topSolutions as any[] | undefined}
          actions={["view"]}
        />
      </div>
    </div>
  );
}

export default AnalyticsPage;
