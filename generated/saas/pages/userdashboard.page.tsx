// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useUserDashboard } from '../hooks/useUserDashboard';
import { Calendar } from 'lucide-react';
import { Badge } from '../../components/ui/badge';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function UserDashboardPage() {
  const { data = {} as any, isLoading } = useUserDashboard();
  const navigate = (to: string) => { window.location.href = to; };
  void isLoading;

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        {/* Section: AssistantStatus (type: status-card) */}
        {/* Section: Channels (type: card-list) */}
        <Card>
          <CardHeader>
            <CardTitle className="text-sm">Today</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-stone-700 whitespace-pre-wrap leading-relaxed">{data?.todaySummary?.summary_text ?? "Your assistant hasn't had any conversations today yet. Send a message to get started! 🦞"}</p>
            <div className="mt-4 pt-3 border-t border-stone-100 flex flex-wrap gap-2">
              <Badge variant="outline">Messages: {data?.todaySummary?.message_count ?? '—'}</Badge>
              <Badge variant="outline">Actions: {data?.todaySummary?.action_count ?? '—'}</Badge>
              <Badge variant="outline">Cost: {data?.todaySummary?.cost_cents ?? '—'}</Badge>
              <Badge variant="outline">Guard Blocks: {data?.todaySummary?.guard_blocks ?? '—'}</Badge>
            </div>
          </CardContent>
        </Card>
        <h3 className="text-sm font-semibold text-stone-700 mb-3">This Week</h3>
        <div className="space-y-4">
          {((data?.recentSummaries) as any[] ?? []).map((entry: any, i: number) => (
            <div key={i} className="flex gap-4">
              <div className="flex flex-col items-center">
                <div className="w-2 h-2 rounded-full bg-stone-400 mt-2 flex-shrink-0" />
                <div className="w-px flex-1 bg-stone-200" />
              </div>
              <div className="pb-4 min-w-0 flex-1">
                <div className="flex items-center gap-2 mb-1">
                  <span className="text-xs font-medium text-stone-500">{entry.date}</span>
                  {entry.mood && <span className="text-sm">{entry.mood}</span>}
                </div>
                <p className="text-sm text-stone-700 line-clamp-3">{entry.summary_text}</p>
                <div className="mt-2 flex flex-wrap gap-1.5">
                  {entry.message_count != null && <Badge variant="outline">msgs: {entry.message_count}</Badge>}
                  {entry.cost_cents != null && <Badge variant="outline">cost: {entry.cost_cents}</Badge>}
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

export default UserDashboardPage;
