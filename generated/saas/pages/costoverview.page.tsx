// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useCostOverview } from '../hooks/useCostOverview';
import { MoreHorizontal } from 'lucide-react';
import { StatCard, Chart, DataTable } from '../components';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function CostOverviewPage() {
  const { data = {} as any, isLoading } = useCostOverview();
  const navigate = (to: string) => { window.location.href = to; };
  void isLoading;

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Total Spend This Month"
                value={data?.cost?.totalThisMonth}
                format="currency"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Last Month"
                value={data?.cost?.totalLastMonth}
                format="currency"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Projected Month-End"
                value={data?.cost?.projectedMonth}
                format="currency"
              />
            </CardContent>
          </Card>
          <Card>
            <CardContent className="p-5">
              <StatCard
                label="Budget Utilization"
                value={data?.cost?.budgetUtilization}
                format="percent"
              />
            </CardContent>
          </Card>
        </div>
        <Chart
          type="area"
          data={data?.cost?.dailySpend}
          title="Spend Over Time"
          xAxis="date"
          yAxis="amount"
        />
        <Chart
          type="donut"
          data={data?.cost?.byModel}
          title="Cost by Model"
          xAxis="model"
          yAxis="cost"
        />
        <Chart
          type="bar"
          data={data?.cost?.byTeam}
          title="Cost by Team"
          xAxis="team"
          yAxis="cost"
        />
        <DataTable
          title="Model Usage Breakdown"
          columns={["model","requests","tokens","costPerToken","totalCost","percentOfTotal"]}
          data={data?.cost?.modelBreakdown as any[] | undefined}
          actions={["view"]}
        />
      </div>
    </div>
  );
}

export default CostOverviewPage;
