// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useGuardEvents } from '../hooks/useGuardEvents';
import { ChevronDown, ChevronRight } from 'lucide-react';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';

export function GuardEventsPage() {
  const { data = {} as any, isLoading } = useGuardEvents();
  const navigate = (to: string) => { window.location.href = to; };
  const [AllEvents, setAllEvents] = React.useState(false);

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        {/* Section: GuardStatus (type: status-card) */}
        {/* Section: RecentBlocks (type: event-list) */}
        <Card>
          <button onClick={() => setAllEvents((v) => !v)} className="w-full flex items-center justify-between px-5 py-4 text-left">
            <span className="text-sm font-semibold text-stone-900">All Guard Events</span>
            {AllEvents ? <ChevronDown className="w-4 h-4 text-stone-400" /> : <ChevronRight className="w-4 h-4 text-stone-400" />}
          </button>
          {AllEvents && (
            <div className="px-5 pb-4">
              <p className="text-sm text-stone-700 whitespace-pre-wrap leading-relaxed">{data?.events?.content ?? ''}</p>
            </div>
          )}
        </Card>
      </div>
    </div>
  );
}

export default GuardEventsPage;
