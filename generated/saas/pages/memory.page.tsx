// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useMemory } from '../hooks/useMemory';
import { ChevronDown, ChevronRight } from 'lucide-react';
import { Badge } from '../../components/ui/badge';
import { Button } from '../../components/ui/button';
import { Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter } from '../../components/ui/card';
import { Input } from '../../components/ui/input';
import { Label } from '../../components/ui/label';
import { Separator } from '../../components/ui/separator';
import { toast } from 'sonner';

export function MemoryPage() {
  const { data = {} as any, isLoading } = useMemory();
  const navigate = (to: string) => { window.location.href = to; };
  const [RawMemory, setRawMemory] = React.useState(false);

  return (
    <div className="min-h-screen bg-stone-50">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-6">
        <Card>
          <CardHeader>
            <CardTitle className="text-sm">Your Assistant's Memory</CardTitle>
          </CardHeader>
          <CardContent>
            <p className="text-sm text-stone-700 whitespace-pre-wrap leading-relaxed">{undefined?.content ?? ""}</p>
          </CardContent>
        </Card>
        <h3 className="text-sm font-semibold text-stone-700 mb-3">MemoryCategories</h3>
        {(() => {
          const items = (data?.memories) as any[] ?? [];
          if (items.length === 0) return <p className="text-sm text-stone-400 py-6 text-center">[object Object]</p>;
          const labels: Record<string, string> = {"decision":"📋 Decisions","preference":"⚙️ Your Preferences","goal":"🎯 Goals","person":"👥 People","date":"📅 Important Dates","boundary":"🚫 Boundaries","insight":"💡 Insights"};
          const groups: Record<string, any[]> = {};
          for (const item of items) { const key = item.category ?? 'other'; if (!groups[key]) groups[key] = []; groups[key].push(item); }
          return (<div className="space-y-4">{Object.entries(groups).map(([key, groupItems]) => (
            <div key={key}>
              <h4 className="text-xs font-semibold text-stone-500 uppercase tracking-wide mb-2">{labels[key] ?? key}</h4>
              <div className="space-y-2">
                {groupItems.map((item: any, i: number) => (
                  <Card key={i}>
                    <CardContent className="flex items-center justify-between px-4 py-3">
                      <div className="min-w-0 flex-1">
                        <p className="text-sm font-medium text-stone-900 truncate">{item.name ?? item.name}</p>
                      </div>
                    </CardContent>
                  </Card>
                ))}
              </div>
            </div>
          ))}</div>);
        })()}
        <div className="min-h-screen flex items-center justify-center bg-stone-50 px-4">
          <div className="w-full max-w-sm">
            <div className="text-center mb-8">
              <div className="w-12 h-12 rounded-2xl mx-auto mb-5 flex items-center justify-center shadow-lg" style={{ backgroundColor: '#18181b' }}><span className="text-white font-bold text-lg">F</span></div>
              <h1 className="text-2xl font-bold text-stone-900">Sign in to Flusk</h1>
              <p className="mt-2 text-sm text-stone-500">Enter your credentials to continue</p>
            </div>
            <Card>
              <CardContent className="pt-6">
                <form onSubmit={(e) => { e.preventDefault(); const fd = new FormData(e.currentTarget); localStorage.setItem('flusk_user', JSON.stringify({ email: fd.get('email'), loggedIn: true })); toast.success('Signed in successfully'); window.location.href = '/app/dashboard'; }} className="space-y-4">
                  <div className="space-y-2">
                    <Label htmlFor="content">What should I remember?</Label>
                    <Input id="content" name="content" type="textarea" placeholder="e.g., I prefer morning meetings, My wife's birthday is June 15th"   />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="category">Category</Label>
                    <Input id="category" name="category" type="select" placeholder=""   />
                  </div>
                </form>
              </CardContent>
            </Card>
          </div>
        </div>
        <Card>
          <button onClick={() => setRawMemory((v) => !v)} className="w-full flex items-center justify-between px-5 py-4 text-left">
            <span className="text-sm font-semibold text-stone-900">Raw MEMORY.md</span>
            {RawMemory ? <ChevronDown className="w-4 h-4 text-stone-400" /> : <ChevronRight className="w-4 h-4 text-stone-400" />}
          </button>
          {RawMemory && (
            <div className="px-5 pb-4">
              <p className="text-sm text-stone-700 whitespace-pre-wrap leading-relaxed">{data?.assistantMemory?.memory ?? ''}</p>
            </div>
          )}
        </Card>
      </div>
    </div>
  );
}

export default MemoryPage;
