// @generated by flusk-lang — DO NOT EDIT
import React, { useState } from 'react';
import { Card, CardContent } from '../../../components/ui/card';
import { Input } from '../../../components/ui/input';
import { Label } from '../../../components/ui/label';
import { Button } from '../../../components/ui/button';

interface WelcomeCardProps {
  onSubmit: (data: { name: string; role: string; company: string }) => void;
}

export function WelcomeCard({ onSubmit }: WelcomeCardProps) {
  const [name, setName] = useState('');
  const [role, setRole] = useState('');
  const [company, setCompany] = useState('');

  const handleSubmit = () => {
    if (name.trim()) {
      onSubmit({ name: name.trim(), role: role.trim(), company: company.trim() });
    }
  };

  return (
    <Card className="border border-stone-200 bg-white shadow-sm w-full max-w-md">
      <CardContent className="pt-6 space-y-5">
        <div className="space-y-1">
          <h3 className="text-lg font-semibold text-stone-900">
            Hey there 👋
          </h3>
          <p className="text-sm text-stone-500">
            I'm FLUSK — your AI assistant that actually has a personality. Let's get to know each other.
          </p>
        </div>

        <div className="space-y-3">
          <div className="space-y-1.5">
            <Label htmlFor="name" className="text-xs font-medium text-stone-700">
              What should I call you?
            </Label>
            <Input
              id="name"
              value={name}
              onChange={(e) => setName(e.target.value)}
              placeholder="Your name"
              className="border-stone-200 focus:border-stone-900 focus:ring-stone-900"
              onKeyDown={(e) => e.key === 'Enter' && handleSubmit()}
            />
          </div>

          <div className="space-y-1.5">
            <Label htmlFor="role" className="text-xs font-medium text-stone-700">
              What do you do?
            </Label>
            <Input
              id="role"
              value={role}
              onChange={(e) => setRole(e.target.value)}
              placeholder="e.g. Product Manager, Developer, CEO"
              className="border-stone-200 focus:border-stone-900 focus:ring-stone-900"
            />
          </div>

          <div className="space-y-1.5">
            <Label htmlFor="company" className="text-xs font-medium text-stone-700">
              Company (optional)
            </Label>
            <Input
              id="company"
              value={company}
              onChange={(e) => setCompany(e.target.value)}
              placeholder="Where do you work?"
              className="border-stone-200 focus:border-stone-900 focus:ring-stone-900"
            />
          </div>
        </div>

        <Button
          onClick={handleSubmit}
          disabled={!name.trim()}
          className="w-full bg-stone-900 hover:bg-stone-800 text-white disabled:bg-stone-300"
        >
          Let's go →
        </Button>
      </CardContent>
    </Card>
  );
}
