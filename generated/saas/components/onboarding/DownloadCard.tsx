// @generated by flusk-lang — DO NOT EDIT
import React, { useState } from 'react';
import { Card, CardContent } from '../../../components/ui/card';
import { Button } from '../../../components/ui/button';
import { Monitor, Download, Check } from 'lucide-react';

const STEPS = [
  'Download the .dmg file',
  'Open and drag FLUSK to Applications',
  'Launch FLUSK from Applications',
  'It will auto-configure with your settings',
];

interface DownloadCardProps {
  onSubmit: () => void;
  downloadUrl?: string;
}

export function DownloadCard({ onSubmit, downloadUrl = 'https://flusk.app/download/mac' }: DownloadCardProps) {
  const [downloaded, setDownloaded] = useState(false);

  const handleDownload = () => {
    window.open(downloadUrl, '_blank');
    setDownloaded(true);
  };

  return (
    <div className="w-full max-w-md space-y-4">
      <div className="space-y-1">
        <h3 className="text-lg font-semibold text-stone-900">
          Install FLUSK on your Mac
        </h3>
        <p className="text-sm text-stone-500">
          One download. Two minutes. Then I live on your machine.
        </p>
      </div>

      <Card className="border border-stone-200 bg-white">
        <CardContent className="pt-6 space-y-5">
          <div className="flex items-center justify-center">
            <div className="w-16 h-16 rounded-2xl bg-stone-900 flex items-center justify-center">
              <Monitor className="w-8 h-8 text-white" />
            </div>
          </div>

          <div className="text-center">
            <p className="text-sm font-medium text-stone-900">FLUSK for macOS</p>
            <p className="text-xs text-stone-500 mt-0.5">Apple Silicon & Intel</p>
          </div>

          <Button
            onClick={handleDownload}
            className="w-full bg-stone-900 hover:bg-stone-800 text-white"
          >
            <Download className="w-4 h-4 mr-2" />
            Download .dmg
          </Button>

          <div className="space-y-2.5 pt-2">
            {STEPS.map((step, i) => (
              <div key={i} className="flex items-start gap-3">
                <span className="flex-shrink-0 w-5 h-5 rounded-full bg-stone-100 text-stone-600 text-xs flex items-center justify-center font-medium">
                  {i + 1}
                </span>
                <span className="text-sm text-stone-600">{step}</span>
              </div>
            ))}
          </div>
        </CardContent>
      </Card>

      <Button
        onClick={onSubmit}
        variant="outline"
        className="w-full border-stone-200 text-stone-700 hover:bg-stone-50"
      >
        {downloaded ? "I've installed it →" : 'Skip for now →'}
      </Button>
    </div>
  );
}
