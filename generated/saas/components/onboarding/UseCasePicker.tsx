// @generated by flusk-lang — DO NOT EDIT
import React, { useState } from 'react';
import { Card, CardContent } from '../../../components/ui/card';
import { Button } from '../../../components/ui/button';
import { Calendar, Bell, Search, Code, PenTool, Home, Check } from 'lucide-react';

const USE_CASES = [
  { id: 'calendar', label: 'Calendar & Scheduling', icon: Calendar },
  { id: 'reminders', label: 'Reminders & Tasks', icon: Bell },
  { id: 'research', label: 'Web Research', icon: Search },
  { id: 'coding', label: 'Code Assistant', icon: Code },
  { id: 'writing', label: 'Writing & Editing', icon: PenTool },
  { id: 'home', label: 'Smart Home', icon: Home },
];

interface UseCasePickerProps {
  onSubmit: (data: { selectedSkills: string[] }) => void;
}

export function UseCasePicker({ onSubmit }: UseCasePickerProps) {
  const [selected, setSelected] = useState<Set<string>>(new Set());

  const toggle = (id: string) => {
    setSelected(prev => {
      const next = new Set(prev);
      if (next.has(id)) next.delete(id);
      else next.add(id);
      return next;
    });
  };

  return (
    <div className="w-full max-w-lg space-y-4">
      <div className="space-y-1">
        <h3 className="text-lg font-semibold text-stone-900">
          What can I help you with?
        </h3>
        <p className="text-sm text-stone-500">
          Pick as many as you want. I'll install the right skills.
        </p>
      </div>

      <div className="grid grid-cols-2 gap-3">
        {USE_CASES.map(({ id, label, icon: Icon }) => {
          const isSelected = selected.has(id);
          return (
            <Card
              key={id}
              onClick={() => toggle(id)}
              className={`cursor-pointer transition-all border ${
                isSelected
                  ? 'bg-stone-900 border-stone-900'
                  : 'bg-white border-stone-200 hover:border-stone-400'
              }`}
            >
              <CardContent className="p-4 flex items-center gap-3">
                <Icon
                  className={`w-5 h-5 flex-shrink-0 ${
                    isSelected ? 'text-white' : 'text-stone-700'
                  }`}
                />
                <span
                  className={`text-sm font-medium ${
                    isSelected ? 'text-white' : 'text-stone-900'
                  }`}
                >
                  {label}
                </span>
                {isSelected && (
                  <Check className="w-4 h-4 text-white ml-auto" />
                )}
              </CardContent>
            </Card>
          );
        })}
      </div>

      <Button
        onClick={() => onSubmit({ selectedSkills: Array.from(selected) })}
        disabled={selected.size === 0}
        className="w-full bg-stone-900 hover:bg-stone-800 text-white disabled:bg-stone-300"
      >
        Continue →
      </Button>
    </div>
  );
}
