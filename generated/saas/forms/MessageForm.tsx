// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { Message } from '../types/Message';

// ── Validation schema ──────────────────────────────────────────────────────
const messageFormSchema = z.object({
  conversation_id: z.string().optional(),
  role: z.enum(['user', 'assistant', 'system']).optional(),
  content: z.string().optional(),
  tokens_used: z.number().optional(),
  cost: z.number().optional(),
  latency_ms: z.number().optional(),
});

export type MessageFormValues = z.infer<typeof messageFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface MessageFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Message>;
  onSubmit: (values: MessageFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function MessageForm({ mode, defaultValues, onSubmit, isLoading }: MessageFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<MessageFormValues>({
    resolver: zodResolver(messageFormSchema),
    defaultValues: defaultValues as MessageFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="conversation_id">Conversation_id</Label>

          <Input id="conversation_id" type="text" {...register('conversation_id')} />
          {errors.conversation_id && <p className="text-sm text-destructive">{errors.conversation_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="role">Role</Label>

          <Controller
            name="role"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="role">
                  <SelectValue placeholder="Select role" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="user">user</SelectItem>
                  <SelectItem value="assistant">assistant</SelectItem>
                  <SelectItem value="system">system</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.role && <p className="text-sm text-destructive">{errors.role?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="content">Content</Label>

          <Input id="content" type="text" {...register('content')} />
          {errors.content && <p className="text-sm text-destructive">{errors.content?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="tokens_used">Tokens_used</Label>

          <Input
            id="tokens_used"
            type="number"
            {...register('tokens_used', { valueAsNumber: true })}
          />
          {errors.tokens_used && <p className="text-sm text-destructive">{errors.tokens_used?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost">Cost</Label>

          <Input
            id="cost"
            type="number"
            {...register('cost', { valueAsNumber: true })}
          />
          {errors.cost && <p className="text-sm text-destructive">{errors.cost?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="latency_ms">Latency_ms</Label>

          <Input
            id="latency_ms"
            type="number"
            {...register('latency_ms', { valueAsNumber: true })}
          />
          {errors.latency_ms && <p className="text-sm text-destructive">{errors.latency_ms?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Message' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default MessageForm;
