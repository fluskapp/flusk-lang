// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { OrgMember } from '../types/OrgMember';

// ── Validation schema ──────────────────────────────────────────────────────
const orgMemberFormSchema = z.object({
  org_id: z.number().int().optional(),
  email: z.string().optional(),
  name: z.string().optional(),
  role: z.enum(['owner', 'admin', 'member', 'viewer']).optional(),
  password_hash: z.string().optional(),
  status: z.enum(['active', 'invited', 'disabled']).optional(),
  last_login_at: z.string().optional(),
});

export type OrgMemberFormValues = z.infer<typeof orgMemberFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface OrgMemberFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<OrgMember>;
  onSubmit: (values: OrgMemberFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function OrgMemberForm({ mode, defaultValues, onSubmit, isLoading }: OrgMemberFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<OrgMemberFormValues>({
    resolver: zodResolver(orgMemberFormSchema),
    defaultValues: defaultValues as OrgMemberFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="email">Email</Label>

          <Input id="email" type="text" {...register('email')} />
          {errors.email && <p className="text-sm text-destructive">{errors.email?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="role">Role</Label>

          <Controller
            name="role"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="role">
                  <SelectValue placeholder="Select role" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="owner">owner</SelectItem>
                  <SelectItem value="admin">admin</SelectItem>
                  <SelectItem value="member">member</SelectItem>
                  <SelectItem value="viewer">viewer</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.role && <p className="text-sm text-destructive">{errors.role?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="password_hash">Password_hash</Label>

          <Input id="password_hash" type="text" {...register('password_hash')} />
          {errors.password_hash && <p className="text-sm text-destructive">{errors.password_hash?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="invited">invited</SelectItem>
                  <SelectItem value="disabled">disabled</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_login_at">Last_login_at</Label>

          <Input id="last_login_at" type="text" {...register('last_login_at')} />
          {errors.last_login_at && <p className="text-sm text-destructive">{errors.last_login_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create OrgMember' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default OrgMemberForm;
