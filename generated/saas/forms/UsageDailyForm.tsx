// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';



import type { UsageDaily } from '../types/UsageDaily';

// ── Validation schema ──────────────────────────────────────────────────────
const usageDailyFormSchema = z.object({
  org_id: z.number().int().optional(),
  device_id: z.string().optional(),
  provider: z.string().optional(),
  model: z.string().optional(),
  date: z.string().optional(),
  request_count: z.number().int().optional(),
  total_input_tokens: z.number().int().optional(),
  total_output_tokens: z.number().int().optional(),
  total_cost_usd: z.number().optional(),
  total_latency_ms: z.number().int().optional(),
  shadow_count: z.number().int().optional(),
});

export type UsageDailyFormValues = z.infer<typeof usageDailyFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface UsageDailyFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<UsageDaily>;
  onSubmit: (values: UsageDailyFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function UsageDailyForm({ mode, defaultValues, onSubmit, isLoading }: UsageDailyFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<UsageDailyFormValues>({
    resolver: zodResolver(usageDailyFormSchema),
    defaultValues: defaultValues as UsageDailyFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="device_id">Device_id</Label>

          <Input id="device_id" type="text" {...register('device_id')} />
          {errors.device_id && <p className="text-sm text-destructive">{errors.device_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="provider">Provider</Label>

          <Input id="provider" type="text" {...register('provider')} />
          {errors.provider && <p className="text-sm text-destructive">{errors.provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="model">Model</Label>

          <Input id="model" type="text" {...register('model')} />
          {errors.model && <p className="text-sm text-destructive">{errors.model?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="date">Date</Label>

          <Input id="date" type="text" {...register('date')} />
          {errors.date && <p className="text-sm text-destructive">{errors.date?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="request_count">Request_count</Label>

          <Input
            id="request_count"
            type="number"
            {...register('request_count', { valueAsNumber: true })}
          />
          {errors.request_count && <p className="text-sm text-destructive">{errors.request_count?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="total_input_tokens">Total_input_tokens</Label>

          <Input
            id="total_input_tokens"
            type="number"
            {...register('total_input_tokens', { valueAsNumber: true })}
          />
          {errors.total_input_tokens && <p className="text-sm text-destructive">{errors.total_input_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="total_output_tokens">Total_output_tokens</Label>

          <Input
            id="total_output_tokens"
            type="number"
            {...register('total_output_tokens', { valueAsNumber: true })}
          />
          {errors.total_output_tokens && <p className="text-sm text-destructive">{errors.total_output_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="total_cost_usd">Total_cost_usd</Label>

          <Input
            id="total_cost_usd"
            type="number"
            {...register('total_cost_usd', { valueAsNumber: true })}
          />
          {errors.total_cost_usd && <p className="text-sm text-destructive">{errors.total_cost_usd?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="total_latency_ms">Total_latency_ms</Label>

          <Input
            id="total_latency_ms"
            type="number"
            {...register('total_latency_ms', { valueAsNumber: true })}
          />
          {errors.total_latency_ms && <p className="text-sm text-destructive">{errors.total_latency_ms?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="shadow_count">Shadow_count</Label>

          <Input
            id="shadow_count"
            type="number"
            {...register('shadow_count', { valueAsNumber: true })}
          />
          {errors.shadow_count && <p className="text-sm text-destructive">{errors.shadow_count?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create UsageDaily' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default UsageDailyForm;
