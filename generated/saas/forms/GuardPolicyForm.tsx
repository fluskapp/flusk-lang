// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';
import { Textarea } from '@/components/ui/textarea';
import type { GuardPolicy } from '../types/GuardPolicy';

// ── Validation schema ──────────────────────────────────────────────────────
const guardPolicyFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  pii_detection: z.boolean().optional(),
  cost_cap_cents: z.number().int().optional(),
  daily_cost_cap_cents: z.number().int().optional(),
  rate_limit_per_minute: z.number().int().optional(),
  blocked_domains: z.string().optional(),
  allowed_tools: z.string().optional(),
  blocked_tools: z.string().optional(),
  require_approval_tools: z.string().optional(),
  intent_matching: z.boolean().optional(),
  sandbox_level: z.enum(['strict', 'standard', 'full']).optional(),
});

export type GuardPolicyFormValues = z.infer<typeof guardPolicyFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface GuardPolicyFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<GuardPolicy>;
  onSubmit: (values: GuardPolicyFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function GuardPolicyForm({ mode, defaultValues, onSubmit, isLoading }: GuardPolicyFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<GuardPolicyFormValues>({
    resolver: zodResolver(guardPolicyFormSchema),
    defaultValues: defaultValues as GuardPolicyFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="pii_detection">Pii_detection</Label>
          <p className="text-xs text-muted-foreground">Scan outbound messages for PII and mask them</p>
          </div>
          <Controller
            name="pii_detection"
            control={control}
            render={({ field: f }) => (
              <Switch id="pii_detection" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.pii_detection && <p className="text-sm text-destructive">{errors.pii_detection?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_cap_cents">Cost_cap_cents</Label>
          <p className="text-xs text-muted-foreground">Monthly cost cap in cents ($50 default)</p>
          <Input
            id="cost_cap_cents"
            type="number"
            {...register('cost_cap_cents', { valueAsNumber: true })}
          />
          {errors.cost_cap_cents && <p className="text-sm text-destructive">{errors.cost_cap_cents?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="daily_cost_cap_cents">Daily_cost_cap_cents</Label>
          <p className="text-xs text-muted-foreground">Daily cost cap in cents (optional, more granular)</p>
          <Input
            id="daily_cost_cap_cents"
            type="number"
            {...register('daily_cost_cap_cents', { valueAsNumber: true })}
          />
          {errors.daily_cost_cap_cents && <p className="text-sm text-destructive">{errors.daily_cost_cap_cents?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="rate_limit_per_minute">Rate_limit_per_minute</Label>

          <Input
            id="rate_limit_per_minute"
            type="number"
            {...register('rate_limit_per_minute', { valueAsNumber: true })}
          />
          {errors.rate_limit_per_minute && <p className="text-sm text-destructive">{errors.rate_limit_per_minute?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="blocked_domains">Blocked_domains</Label>
          <p className="text-xs text-muted-foreground">Domains the assistant cannot access</p>
          <Textarea id="blocked_domains" {...register('blocked_domains')} />
          {errors.blocked_domains && <p className="text-sm text-destructive">{errors.blocked_domains?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="allowed_tools">Allowed_tools</Label>
          <p className="text-xs text-muted-foreground">Tools the assistant is allowed to use</p>
          <Textarea id="allowed_tools" {...register('allowed_tools')} />
          {errors.allowed_tools && <p className="text-sm text-destructive">{errors.allowed_tools?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="blocked_tools">Blocked_tools</Label>
          <p className="text-xs text-muted-foreground">Tools explicitly denied</p>
          <Textarea id="blocked_tools" {...register('blocked_tools')} />
          {errors.blocked_tools && <p className="text-sm text-destructive">{errors.blocked_tools?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="require_approval_tools">Require_approval_tools</Label>
          <p className="text-xs text-muted-foreground">Tools that need user confirmation before executing</p>
          <Textarea id="require_approval_tools" {...register('require_approval_tools')} />
          {errors.require_approval_tools && <p className="text-sm text-destructive">{errors.require_approval_tools?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="intent_matching">Intent_matching</Label>
          <p className="text-xs text-muted-foreground">Check if tool calls match the user's original intent</p>
          </div>
          <Controller
            name="intent_matching"
            control={control}
            render={({ field: f }) => (
              <Switch id="intent_matching" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.intent_matching && <p className="text-sm text-destructive">{errors.intent_matching?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="sandbox_level">Sandbox_level</Label>
          <p className="text-xs text-muted-foreground">strict=no network, standard=limited network, full=unrestricted</p>
          <Controller
            name="sandbox_level"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="sandbox_level">
                  <SelectValue placeholder="Select sandbox_level" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="strict">strict</SelectItem>
                  <SelectItem value="standard">standard</SelectItem>
                  <SelectItem value="full">full</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.sandbox_level && <p className="text-sm text-destructive">{errors.sandbox_level?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create GuardPolicy' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default GuardPolicyForm;
