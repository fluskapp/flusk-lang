// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { Organization } from '../types/Organization';

// ── Validation schema ──────────────────────────────────────────────────────
const organizationFormSchema = z.object({
  name: z.string().optional(),
  slug: z.string().optional(),
  plan: z.enum(['starter', 'growth', 'enterprise']).optional(),
  max_seats: z.number().int().optional(),
  api_key: z.string().optional(),
  status: z.enum(['active', 'suspended', 'trial']).optional(),
  trial_ends_at: z.string().optional(),
});

export type OrganizationFormValues = z.infer<typeof organizationFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface OrganizationFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Organization>;
  onSubmit: (values: OrganizationFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function OrganizationForm({ mode, defaultValues, onSubmit, isLoading }: OrganizationFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<OrganizationFormValues>({
    resolver: zodResolver(organizationFormSchema),
    defaultValues: defaultValues as OrganizationFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="slug">Slug</Label>

          <Input id="slug" type="text" {...register('slug')} />
          {errors.slug && <p className="text-sm text-destructive">{errors.slug?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="plan">Plan</Label>

          <Controller
            name="plan"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="plan">
                  <SelectValue placeholder="Select plan" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="starter">starter</SelectItem>
                  <SelectItem value="growth">growth</SelectItem>
                  <SelectItem value="enterprise">enterprise</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.plan && <p className="text-sm text-destructive">{errors.plan?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="max_seats">Max_seats</Label>

          <Input
            id="max_seats"
            type="number"
            {...register('max_seats', { valueAsNumber: true })}
          />
          {errors.max_seats && <p className="text-sm text-destructive">{errors.max_seats?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="api_key">Api_key</Label>
          <p className="text-xs text-muted-foreground">encrypted: api_key</p>
          <Input id="api_key" type="text" {...register('api_key')} />
          {errors.api_key && <p className="text-sm text-destructive">{errors.api_key?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="suspended">suspended</SelectItem>
                  <SelectItem value="trial">trial</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="trial_ends_at">Trial_ends_at</Label>

          <Input id="trial_ends_at" type="text" {...register('trial_ends_at')} />
          {errors.trial_ends_at && <p className="text-sm text-destructive">{errors.trial_ends_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Organization' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default OrganizationForm;
