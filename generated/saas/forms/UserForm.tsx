// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';

import type { User } from '../types/User';

// ── Validation schema ──────────────────────────────────────────────────────
const userFormSchema = z.object({
  account_id: z.number().int().optional(),
  email: z.string().optional(),
  name: z.string().optional(),
  avatar_url: z.string().optional(),
  role: z.enum(['owner', 'admin', 'user', 'viewer']).optional(),
  department: z.string().optional(),
  job_title: z.string().optional(),
  sso_id: z.string().optional(),
  status: z.enum(['active', 'invited', 'disabled', 'suspended']).optional(),
  last_login_at: z.string().optional(),
  onboarding_complete: z.boolean().optional(),
  flusk_key: z.string().optional(),
});

export type UserFormValues = z.infer<typeof userFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface UserFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<User>;
  onSubmit: (values: UserFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function UserForm({ mode, defaultValues, onSubmit, isLoading }: UserFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<UserFormValues>({
    resolver: zodResolver(userFormSchema),
    defaultValues: defaultValues as UserFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="account_id">Account_id</Label>

          <Input
            id="account_id"
            type="number"
            {...register('account_id', { valueAsNumber: true })}
          />
          {errors.account_id && <p className="text-sm text-destructive">{errors.account_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="email">Email</Label>

          <Input id="email" type="text" {...register('email')} />
          {errors.email && <p className="text-sm text-destructive">{errors.email?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="avatar_url">Avatar_url</Label>

          <Input id="avatar_url" type="text" {...register('avatar_url')} />
          {errors.avatar_url && <p className="text-sm text-destructive">{errors.avatar_url?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="role">Role</Label>

          <Controller
            name="role"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="role">
                  <SelectValue placeholder="Select role" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="owner">owner</SelectItem>
                  <SelectItem value="admin">admin</SelectItem>
                  <SelectItem value="user">user</SelectItem>
                  <SelectItem value="viewer">viewer</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.role && <p className="text-sm text-destructive">{errors.role?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="department">Department</Label>
          <p className="text-xs text-muted-foreground">Organization accounts only</p>
          <Input id="department" type="text" {...register('department')} />
          {errors.department && <p className="text-sm text-destructive">{errors.department?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="job_title">Job_title</Label>

          <Input id="job_title" type="text" {...register('job_title')} />
          {errors.job_title && <p className="text-sm text-destructive">{errors.job_title?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="sso_id">Sso_id</Label>

          <Input id="sso_id" type="text" {...register('sso_id')} />
          {errors.sso_id && <p className="text-sm text-destructive">{errors.sso_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="invited">invited</SelectItem>
                  <SelectItem value="disabled">disabled</SelectItem>
                  <SelectItem value="suspended">suspended</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_login_at">Last_login_at</Label>

          <Input id="last_login_at" type="text" {...register('last_login_at')} />
          {errors.last_login_at && <p className="text-sm text-destructive">{errors.last_login_at?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="onboarding_complete">Onboarding_complete</Label>

          </div>
          <Controller
            name="onboarding_complete"
            control={control}
            render={({ field: f }) => (
              <Switch id="onboarding_complete" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.onboarding_complete && <p className="text-sm text-destructive">{errors.onboarding_complete?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="flusk_key">Flusk_key</Label>
          <p className="text-xs text-muted-foreground">API key for local OpenClaw installs to connect to Flusk</p>
          <Input id="flusk_key" type="text" {...register('flusk_key')} />
          {errors.flusk_key && <p className="text-sm text-destructive">{errors.flusk_key?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create User' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default UserForm;
