// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { Schedule } from '../types/Schedule';

// ── Validation schema ──────────────────────────────────────────────────────
const scheduleFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  name: z.string().optional(),
  cron_expression: z.string().optional(),
  interval_ms: z.number().int().optional(),
  prompt: z.string().optional(),
  timezone: z.string().optional(),
  last_run_at: z.string().optional(),
  next_run_at: z.string().optional(),
  run_count: z.number().int().optional(),
  status: z.enum(['active', 'paused', 'error']).optional(),
  delivery: z.enum(['channel', 'silent', 'webhook']).optional(),
});

export type ScheduleFormValues = z.infer<typeof scheduleFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface ScheduleFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Schedule>;
  onSubmit: (values: ScheduleFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function ScheduleForm({ mode, defaultValues, onSubmit, isLoading }: ScheduleFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<ScheduleFormValues>({
    resolver: zodResolver(scheduleFormSchema),
    defaultValues: defaultValues as ScheduleFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cron_expression">Cron_expression</Label>
          <p className="text-xs text-muted-foreground">Standard cron expression (e.g. '0 9 * * 1-5')</p>
          <Input id="cron_expression" type="text" {...register('cron_expression')} />
          {errors.cron_expression && <p className="text-sm text-destructive">{errors.cron_expression?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="interval_ms">Interval_ms</Label>
          <p className="text-xs text-muted-foreground">Repeat interval in milliseconds (alternative to cron_expression)</p>
          <Input
            id="interval_ms"
            type="number"
            {...register('interval_ms', { valueAsNumber: true })}
          />
          {errors.interval_ms && <p className="text-sm text-destructive">{errors.interval_ms?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="prompt">Prompt</Label>
          <p className="text-xs text-muted-foreground">Prompt to send to the assistant when the schedule fires</p>
          <Input id="prompt" type="text" {...register('prompt')} />
          {errors.prompt && <p className="text-sm text-destructive">{errors.prompt?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="timezone">Timezone</Label>

          <Input id="timezone" type="text" {...register('timezone')} />
          {errors.timezone && <p className="text-sm text-destructive">{errors.timezone?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_run_at">Last_run_at</Label>

          <Input id="last_run_at" type="text" {...register('last_run_at')} />
          {errors.last_run_at && <p className="text-sm text-destructive">{errors.last_run_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="next_run_at">Next_run_at</Label>

          <Input id="next_run_at" type="text" {...register('next_run_at')} />
          {errors.next_run_at && <p className="text-sm text-destructive">{errors.next_run_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="run_count">Run_count</Label>

          <Input
            id="run_count"
            type="number"
            {...register('run_count', { valueAsNumber: true })}
          />
          {errors.run_count && <p className="text-sm text-destructive">{errors.run_count?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="paused">paused</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="delivery">Delivery</Label>
          <p className="text-xs text-muted-foreground">Where to deliver the assistant response</p>
          <Controller
            name="delivery"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="delivery">
                  <SelectValue placeholder="Select delivery" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="channel">channel</SelectItem>
                  <SelectItem value="silent">silent</SelectItem>
                  <SelectItem value="webhook">webhook</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.delivery && <p className="text-sm text-destructive">{errors.delivery?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Schedule' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default ScheduleForm;
