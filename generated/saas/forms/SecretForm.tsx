// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';



import type { Secret } from '../types/Secret';

// ── Validation schema ──────────────────────────────────────────────────────
const secretFormSchema = z.object({
  account_id: z.number().int().optional(),
  assistant_id: z.number().int().optional(),
  key: z.string().optional(),
  encrypted_value: z.string().optional(),
  provider: z.string().optional(),
  expires_at: z.string().optional(),
  rotated_at: z.string().optional(),
});

export type SecretFormValues = z.infer<typeof secretFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface SecretFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Secret>;
  onSubmit: (values: SecretFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function SecretForm({ mode, defaultValues, onSubmit, isLoading }: SecretFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<SecretFormValues>({
    resolver: zodResolver(secretFormSchema),
    defaultValues: defaultValues as SecretFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="account_id">Account_id</Label>

          <Input
            id="account_id"
            type="number"
            {...register('account_id', { valueAsNumber: true })}
          />
          {errors.account_id && <p className="text-sm text-destructive">{errors.account_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>
          <p className="text-xs text-muted-foreground">Scoped to a specific assistant, or null for account-wide</p>
          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="key">Key</Label>
          <p className="text-xs text-muted-foreground">Identifier for this secret (e.g. llm_api_key, stripe_secret)</p>
          <Input id="key" type="text" {...register('key')} />
          {errors.key && <p className="text-sm text-destructive">{errors.key?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="encrypted_value">Encrypted_value</Label>
          <p className="text-xs text-muted-foreground">AES-256-GCM encrypted secret value</p>
          <Input id="encrypted_value" type="text" {...register('encrypted_value')} />
          {errors.encrypted_value && <p className="text-sm text-destructive">{errors.encrypted_value?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="provider">Provider</Label>
          <p className="text-xs text-muted-foreground">Service provider this secret belongs to</p>
          <Input id="provider" type="text" {...register('provider')} />
          {errors.provider && <p className="text-sm text-destructive">{errors.provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="expires_at">Expires_at</Label>

          <Input id="expires_at" type="text" {...register('expires_at')} />
          {errors.expires_at && <p className="text-sm text-destructive">{errors.expires_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="rotated_at">Rotated_at</Label>

          <Input id="rotated_at" type="text" {...register('rotated_at')} />
          {errors.rotated_at && <p className="text-sm text-destructive">{errors.rotated_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Secret' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default SecretForm;
