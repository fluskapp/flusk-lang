// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';
import { Textarea } from '@/components/ui/textarea';
import type { GuardEvent } from '../types/GuardEvent';

// ── Validation schema ──────────────────────────────────────────────────────
const guardEventFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  event_type: z.enum(['message_in', 'message_out', 'tool_call', 'action', 'cost', 'error', 'warning', 'guard_block', 'pii_detected', 'anomaly']).optional(),
  severity: z.enum(['info', 'warning', 'error', 'critical']).optional(),
  channel: z.string().optional(),
  details: z.string().optional(),
  cost_cents: z.number().int().optional(),
  tokens_used: z.number().int().optional(),
  model_used: z.string().optional(),
  blocked: z.boolean().optional(),
});

export type GuardEventFormValues = z.infer<typeof guardEventFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface GuardEventFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<GuardEvent>;
  onSubmit: (values: GuardEventFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function GuardEventForm({ mode, defaultValues, onSubmit, isLoading }: GuardEventFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<GuardEventFormValues>({
    resolver: zodResolver(guardEventFormSchema),
    defaultValues: defaultValues as GuardEventFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="event_type">Event_type</Label>

          <Controller
            name="event_type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="event_type">
                  <SelectValue placeholder="Select event_type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="message_in">message_in</SelectItem>
                  <SelectItem value="message_out">message_out</SelectItem>
                  <SelectItem value="tool_call">tool_call</SelectItem>
                  <SelectItem value="action">action</SelectItem>
                  <SelectItem value="cost">cost</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                  <SelectItem value="warning">warning</SelectItem>
                  <SelectItem value="guard_block">guard_block</SelectItem>
                  <SelectItem value="pii_detected">pii_detected</SelectItem>
                  <SelectItem value="anomaly">anomaly</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.event_type && <p className="text-sm text-destructive">{errors.event_type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="severity">Severity</Label>

          <Controller
            name="severity"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="severity">
                  <SelectValue placeholder="Select severity" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="info">info</SelectItem>
                  <SelectItem value="warning">warning</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                  <SelectItem value="critical">critical</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.severity && <p className="text-sm text-destructive">{errors.severity?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channel">Channel</Label>
          <p className="text-xs text-muted-foreground">Channel identifier where the event originated</p>
          <Input id="channel" type="text" {...register('channel')} />
          {errors.channel && <p className="text-sm text-destructive">{errors.channel?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="details">Details</Label>
          <p className="text-xs text-muted-foreground">Event-specific payload data</p>
          <Textarea id="details" {...register('details')} />
          {errors.details && <p className="text-sm text-destructive">{errors.details?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_cents">Cost_cents</Label>
          <p className="text-xs text-muted-foreground">Cost of this event in cents</p>
          <Input
            id="cost_cents"
            type="number"
            {...register('cost_cents', { valueAsNumber: true })}
          />
          {errors.cost_cents && <p className="text-sm text-destructive">{errors.cost_cents?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="tokens_used">Tokens_used</Label>

          <Input
            id="tokens_used"
            type="number"
            {...register('tokens_used', { valueAsNumber: true })}
          />
          {errors.tokens_used && <p className="text-sm text-destructive">{errors.tokens_used?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="model_used">Model_used</Label>

          <Input id="model_used" type="text" {...register('model_used')} />
          {errors.model_used && <p className="text-sm text-destructive">{errors.model_used?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="blocked">Blocked</Label>
          <p className="text-xs text-muted-foreground">Whether this event was blocked by a guard rule</p>
          </div>
          <Controller
            name="blocked"
            control={control}
            render={({ field: f }) => (
              <Switch id="blocked" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.blocked && <p className="text-sm text-destructive">{errors.blocked?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create GuardEvent' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default GuardEventForm;
