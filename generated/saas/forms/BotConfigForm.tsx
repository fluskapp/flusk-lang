// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';

import { Switch } from '@/components/ui/switch';

import type { BotConfig } from '../types/BotConfig';

// ── Validation schema ──────────────────────────────────────────────────────
const botConfigFormSchema = z.object({
  workspace_id: z.string().optional(),
  bot_token: z.string().optional(),
  active: z.boolean().optional(),
});

export type BotConfigFormValues = z.infer<typeof botConfigFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface BotConfigFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<BotConfig>;
  onSubmit: (values: BotConfigFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function BotConfigForm({ mode, defaultValues, onSubmit, isLoading }: BotConfigFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<BotConfigFormValues>({
    resolver: zodResolver(botConfigFormSchema),
    defaultValues: defaultValues as BotConfigFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="workspace_id">Workspace_id</Label>

          <Input id="workspace_id" type="text" {...register('workspace_id')} />
          {errors.workspace_id && <p className="text-sm text-destructive">{errors.workspace_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="bot_token">Bot_token</Label>

          <Input id="bot_token" type="text" {...register('bot_token')} />
          {errors.bot_token && <p className="text-sm text-destructive">{errors.bot_token?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="active">Active</Label>

          </div>
          <Controller
            name="active"
            control={control}
            render={({ field: f }) => (
              <Switch id="active" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.active && <p className="text-sm text-destructive">{errors.active?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create BotConfig' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default BotConfigForm;
