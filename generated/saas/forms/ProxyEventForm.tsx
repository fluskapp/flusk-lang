// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';
import { Textarea } from '@/components/ui/textarea';
import type { ProxyEvent } from '../types/ProxyEvent';

// ── Validation schema ──────────────────────────────────────────────────────
const proxyEventFormSchema = z.object({
  bot_id: z.number().int().optional(),
  direction: z.enum(['inbound', 'outbound', 'tool_call', 'internal']).optional(),
  event_type: z.enum(['message', 'tool_call', 'api_call', 'pii_detected', 'pii_masked', 'policy_block', 'rate_limit', 'cost_alert', 'error']).optional(),
  channel: z.string().optional(),
  content_preview: z.string().optional(),
  details: z.string().optional(),
  cost_usd: z.number().optional(),
  tokens: z.number().int().optional(),
  model: z.string().optional(),
  blocked: z.boolean().optional(),
  pii_found: z.boolean().optional(),
  pii_types: z.string().optional(),
  latency_ms: z.number().int().optional(),
  session_id: z.string().optional(),
});

export type ProxyEventFormValues = z.infer<typeof proxyEventFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface ProxyEventFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<ProxyEvent>;
  onSubmit: (values: ProxyEventFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function ProxyEventForm({ mode, defaultValues, onSubmit, isLoading }: ProxyEventFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<ProxyEventFormValues>({
    resolver: zodResolver(proxyEventFormSchema),
    defaultValues: defaultValues as ProxyEventFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="bot_id">Bot_id</Label>
          <p className="text-xs text-muted-foreground">The bot this event belongs to</p>
          <Input
            id="bot_id"
            type="number"
            {...register('bot_id', { valueAsNumber: true })}
          />
          {errors.bot_id && <p className="text-sm text-destructive">{errors.bot_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="direction">Direction</Label>

          <Controller
            name="direction"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="direction">
                  <SelectValue placeholder="Select direction" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="inbound">inbound</SelectItem>
                  <SelectItem value="outbound">outbound</SelectItem>
                  <SelectItem value="tool_call">tool_call</SelectItem>
                  <SelectItem value="internal">internal</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.direction && <p className="text-sm text-destructive">{errors.direction?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="event_type">Event_type</Label>

          <Controller
            name="event_type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="event_type">
                  <SelectValue placeholder="Select event_type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="message">message</SelectItem>
                  <SelectItem value="tool_call">tool_call</SelectItem>
                  <SelectItem value="api_call">api_call</SelectItem>
                  <SelectItem value="pii_detected">pii_detected</SelectItem>
                  <SelectItem value="pii_masked">pii_masked</SelectItem>
                  <SelectItem value="policy_block">policy_block</SelectItem>
                  <SelectItem value="rate_limit">rate_limit</SelectItem>
                  <SelectItem value="cost_alert">cost_alert</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.event_type && <p className="text-sm text-destructive">{errors.event_type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channel">Channel</Label>
          <p className="text-xs text-muted-foreground">whatsapp, telegram, web, api</p>
          <Input id="channel" type="text" {...register('channel')} />
          {errors.channel && <p className="text-sm text-destructive">{errors.channel?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="content_preview">Content_preview</Label>
          <p className="text-xs text-muted-foreground">Human-readable summary of what happened (cynical tone)</p>
          <Input id="content_preview" type="text" {...register('content_preview')} />
          {errors.content_preview && <p className="text-sm text-destructive">{errors.content_preview?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="details">Details</Label>
          <p className="text-xs text-muted-foreground">Full event payload</p>
          <Textarea id="details" {...register('details')} />
          {errors.details && <p className="text-sm text-destructive">{errors.details?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_usd">Cost_usd</Label>

          <Input
            id="cost_usd"
            type="number"
            {...register('cost_usd', { valueAsNumber: true })}
          />
          {errors.cost_usd && <p className="text-sm text-destructive">{errors.cost_usd?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="tokens">Tokens</Label>

          <Input
            id="tokens"
            type="number"
            {...register('tokens', { valueAsNumber: true })}
          />
          {errors.tokens && <p className="text-sm text-destructive">{errors.tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="model">Model</Label>

          <Input id="model" type="text" {...register('model')} />
          {errors.model && <p className="text-sm text-destructive">{errors.model?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="blocked">Blocked</Label>

          </div>
          <Controller
            name="blocked"
            control={control}
            render={({ field: f }) => (
              <Switch id="blocked" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.blocked && <p className="text-sm text-destructive">{errors.blocked?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="pii_found">Pii_found</Label>

          </div>
          <Controller
            name="pii_found"
            control={control}
            render={({ field: f }) => (
              <Switch id="pii_found" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.pii_found && <p className="text-sm text-destructive">{errors.pii_found?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="pii_types">Pii_types</Label>
          <p className="text-xs text-muted-foreground">Array of PII types found (e.g. ["email", "israeli_id"])</p>
          <Textarea id="pii_types" {...register('pii_types')} />
          {errors.pii_types && <p className="text-sm text-destructive">{errors.pii_types?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="latency_ms">Latency_ms</Label>

          <Input
            id="latency_ms"
            type="number"
            {...register('latency_ms', { valueAsNumber: true })}
          />
          {errors.latency_ms && <p className="text-sm text-destructive">{errors.latency_ms?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="session_id">Session_id</Label>

          <Input id="session_id" type="text" {...register('session_id')} />
          {errors.session_id && <p className="text-sm text-destructive">{errors.session_id?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create ProxyEvent' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default ProxyEventForm;
