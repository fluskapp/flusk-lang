// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { Assistant } from '../types/Assistant';

// ── Validation schema ──────────────────────────────────────────────────────
const assistantFormSchema = z.object({
  account_id: z.number().int().optional(),
  user_id: z.number().int().optional(),
  name: z.string().optional(),
  slug: z.string().optional(),
  soul: z.string().optional(),
  identity: z.string().optional(),
  user_context: z.string().optional(),
  memory: z.string().optional(),
  llm_provider: z.enum(['anthropic', 'openai', 'google', 'azure_openai', 'custom']).optional(),
  llm_model: z.string().optional(),
  temperature: z.number().optional(),
  machine_tier: z.enum(['starter', 'pro', 'power', 'local']).optional(),
  container_id: z.string().optional(),
  container_status: z.enum(['pending', 'provisioning', 'running', 'stopped', 'error']).optional(),
  openclaw_version: z.string().optional(),
  tools_enabled: z.string().optional(),
  sandbox_level: z.enum(['strict', 'standard', 'full']).optional(),
  status: z.enum(['active', 'paused', 'provisioning', 'error']).optional(),
});

export type AssistantFormValues = z.infer<typeof assistantFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface AssistantFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Assistant>;
  onSubmit: (values: AssistantFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function AssistantForm({ mode, defaultValues, onSubmit, isLoading }: AssistantFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<AssistantFormValues>({
    resolver: zodResolver(assistantFormSchema),
    defaultValues: defaultValues as AssistantFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="account_id">Account_id</Label>

          <Input
            id="account_id"
            type="number"
            {...register('account_id', { valueAsNumber: true })}
          />
          {errors.account_id && <p className="text-sm text-destructive">{errors.account_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="user_id">User_id</Label>

          <Input
            id="user_id"
            type="number"
            {...register('user_id', { valueAsNumber: true })}
          />
          {errors.user_id && <p className="text-sm text-destructive">{errors.user_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="slug">Slug</Label>

          <Input id="slug" type="text" {...register('slug')} />
          {errors.slug && <p className="text-sm text-destructive">{errors.slug?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="soul">Soul</Label>
          <p className="text-xs text-muted-foreground">SOUL.md content — assistant personality and core values</p>
          <Input id="soul" type="text" {...register('soul')} />
          {errors.soul && <p className="text-sm text-destructive">{errors.soul?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="identity">Identity</Label>
          <p className="text-xs text-muted-foreground">IDENTITY.md structured data — name, role, traits</p>
          <Textarea id="identity" {...register('identity')} />
          {errors.identity && <p className="text-sm text-destructive">{errors.identity?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="user_context">User_context</Label>
          <p className="text-xs text-muted-foreground">USER.md content — context about the user this assistant serves</p>
          <Input id="user_context" type="text" {...register('user_context')} />
          {errors.user_context && <p className="text-sm text-destructive">{errors.user_context?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="memory">Memory</Label>
          <p className="text-xs text-muted-foreground">MEMORY.md content — persistent memory across sessions</p>
          <Input id="memory" type="text" {...register('memory')} />
          {errors.memory && <p className="text-sm text-destructive">{errors.memory?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="llm_provider">Llm_provider</Label>

          <Controller
            name="llm_provider"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="llm_provider">
                  <SelectValue placeholder="Select llm_provider" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="anthropic">anthropic</SelectItem>
                  <SelectItem value="openai">openai</SelectItem>
                  <SelectItem value="google">google</SelectItem>
                  <SelectItem value="azure_openai">azure_openai</SelectItem>
                  <SelectItem value="custom">custom</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.llm_provider && <p className="text-sm text-destructive">{errors.llm_provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="llm_model">Llm_model</Label>

          <Input id="llm_model" type="text" {...register('llm_model')} />
          {errors.llm_model && <p className="text-sm text-destructive">{errors.llm_model?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="temperature">Temperature</Label>

          <Input
            id="temperature"
            type="number"
            {...register('temperature', { valueAsNumber: true })}
          />
          {errors.temperature && <p className="text-sm text-destructive">{errors.temperature?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="machine_tier">Machine_tier</Label>
          <p className="text-xs text-muted-foreground">VPS/container size tier</p>
          <Controller
            name="machine_tier"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="machine_tier">
                  <SelectValue placeholder="Select machine_tier" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="starter">starter</SelectItem>
                  <SelectItem value="pro">pro</SelectItem>
                  <SelectItem value="power">power</SelectItem>
                  <SelectItem value="local">local</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.machine_tier && <p className="text-sm text-destructive">{errors.machine_tier?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="container_id">Container_id</Label>
          <p className="text-xs text-muted-foreground">VPS or container identifier</p>
          <Input id="container_id" type="text" {...register('container_id')} />
          {errors.container_id && <p className="text-sm text-destructive">{errors.container_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="container_status">Container_status</Label>

          <Controller
            name="container_status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="container_status">
                  <SelectValue placeholder="Select container_status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="pending">pending</SelectItem>
                  <SelectItem value="provisioning">provisioning</SelectItem>
                  <SelectItem value="running">running</SelectItem>
                  <SelectItem value="stopped">stopped</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.container_status && <p className="text-sm text-destructive">{errors.container_status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="openclaw_version">Openclaw_version</Label>

          <Input id="openclaw_version" type="text" {...register('openclaw_version')} />
          {errors.openclaw_version && <p className="text-sm text-destructive">{errors.openclaw_version?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="tools_enabled">Tools_enabled</Label>
          <p className="text-xs text-muted-foreground">List of enabled tool identifiers</p>
          <Textarea id="tools_enabled" {...register('tools_enabled')} />
          {errors.tools_enabled && <p className="text-sm text-destructive">{errors.tools_enabled?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="sandbox_level">Sandbox_level</Label>

          <Controller
            name="sandbox_level"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="sandbox_level">
                  <SelectValue placeholder="Select sandbox_level" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="strict">strict</SelectItem>
                  <SelectItem value="standard">standard</SelectItem>
                  <SelectItem value="full">full</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.sandbox_level && <p className="text-sm text-destructive">{errors.sandbox_level?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="paused">paused</SelectItem>
                  <SelectItem value="provisioning">provisioning</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Assistant' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default AssistantForm;
