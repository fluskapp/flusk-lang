// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { SkillInstall } from '../types/SkillInstall';

// ── Validation schema ──────────────────────────────────────────────────────
const skillInstallFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  skill_id: z.string().optional(),
  skill_version: z.string().optional(),
  config: z.string().optional(),
  status: z.enum(['active', 'disabled', 'error']).optional(),
});

export type SkillInstallFormValues = z.infer<typeof skillInstallFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface SkillInstallFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<SkillInstall>;
  onSubmit: (values: SkillInstallFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function SkillInstallForm({ mode, defaultValues, onSubmit, isLoading }: SkillInstallFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<SkillInstallFormValues>({
    resolver: zodResolver(skillInstallFormSchema),
    defaultValues: defaultValues as SkillInstallFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="skill_id">Skill_id</Label>
          <p className="text-xs text-muted-foreground">Marketplace skill identifier</p>
          <Input id="skill_id" type="text" {...register('skill_id')} />
          {errors.skill_id && <p className="text-sm text-destructive">{errors.skill_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="skill_version">Skill_version</Label>

          <Input id="skill_version" type="text" {...register('skill_version')} />
          {errors.skill_version && <p className="text-sm text-destructive">{errors.skill_version?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="config">Config</Label>
          <p className="text-xs text-muted-foreground">Skill-specific configuration overrides</p>
          <Textarea id="config" {...register('config')} />
          {errors.config && <p className="text-sm text-destructive">{errors.config?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="disabled">disabled</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create SkillInstall' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default SkillInstallForm;
