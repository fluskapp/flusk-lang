// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { KnowledgeBase } from '../types/KnowledgeBase';

// ── Validation schema ──────────────────────────────────────────────────────
const knowledgeBaseFormSchema = z.object({
  solution_id: z.number().int().optional(),
  name: z.string().optional(),
  type: z.enum(['file', 'url', 'text', 'api']).optional(),
  source: z.string().optional(),
  content_hash: z.string().optional(),
  chunk_count: z.number().int().optional(),
  status: z.enum(['pending', 'processing', 'ready', 'error']).optional(),
  error_message: z.string().optional(),
});

export type KnowledgeBaseFormValues = z.infer<typeof knowledgeBaseFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface KnowledgeBaseFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<KnowledgeBase>;
  onSubmit: (values: KnowledgeBaseFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function KnowledgeBaseForm({ mode, defaultValues, onSubmit, isLoading }: KnowledgeBaseFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<KnowledgeBaseFormValues>({
    resolver: zodResolver(knowledgeBaseFormSchema),
    defaultValues: defaultValues as KnowledgeBaseFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="solution_id">Solution_id</Label>

          <Input
            id="solution_id"
            type="number"
            {...register('solution_id', { valueAsNumber: true })}
          />
          {errors.solution_id && <p className="text-sm text-destructive">{errors.solution_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="type">Type</Label>

          <Controller
            name="type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="type">
                  <SelectValue placeholder="Select type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="file">file</SelectItem>
                  <SelectItem value="url">url</SelectItem>
                  <SelectItem value="text">text</SelectItem>
                  <SelectItem value="api">api</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.type && <p className="text-sm text-destructive">{errors.type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="source">Source</Label>
          <p className="text-xs text-muted-foreground">URL, file path, or raw text</p>
          <Input id="source" type="text" {...register('source')} />
          {errors.source && <p className="text-sm text-destructive">{errors.source?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="content_hash">Content_hash</Label>

          <Input id="content_hash" type="text" {...register('content_hash')} />
          {errors.content_hash && <p className="text-sm text-destructive">{errors.content_hash?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="chunk_count">Chunk_count</Label>

          <Input
            id="chunk_count"
            type="number"
            {...register('chunk_count', { valueAsNumber: true })}
          />
          {errors.chunk_count && <p className="text-sm text-destructive">{errors.chunk_count?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="pending">pending</SelectItem>
                  <SelectItem value="processing">processing</SelectItem>
                  <SelectItem value="ready">ready</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="error_message">Error_message</Label>

          <Input id="error_message" type="text" {...register('error_message')} />
          {errors.error_message && <p className="text-sm text-destructive">{errors.error_message?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create KnowledgeBase' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default KnowledgeBaseForm;
