// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { WebhookConfig } from '../types/WebhookConfig';

// ── Validation schema ──────────────────────────────────────────────────────
const webhookConfigFormSchema = z.object({
  bot_id: z.string().optional(),
  verify_token: z.string().optional(),
  api_secret: z.string().optional(),
  provider: z.enum(['waha', 'twilio', 'meta-cloud']).optional(),
  webhook_url: z.string().optional(),
});

export type WebhookConfigFormValues = z.infer<typeof webhookConfigFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface WebhookConfigFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<WebhookConfig>;
  onSubmit: (values: WebhookConfigFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function WebhookConfigForm({ mode, defaultValues, onSubmit, isLoading }: WebhookConfigFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<WebhookConfigFormValues>({
    resolver: zodResolver(webhookConfigFormSchema),
    defaultValues: defaultValues as WebhookConfigFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="bot_id">Bot_id</Label>

          <Input id="bot_id" type="text" {...register('bot_id')} />
          {errors.bot_id && <p className="text-sm text-destructive">{errors.bot_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="verify_token">Verify_token</Label>

          <Input id="verify_token" type="text" {...register('verify_token')} />
          {errors.verify_token && <p className="text-sm text-destructive">{errors.verify_token?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="api_secret">Api_secret</Label>

          <Input id="api_secret" type="text" {...register('api_secret')} />
          {errors.api_secret && <p className="text-sm text-destructive">{errors.api_secret?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="provider">Provider</Label>

          <Controller
            name="provider"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="provider">
                  <SelectValue placeholder="Select provider" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="waha">waha</SelectItem>
                  <SelectItem value="twilio">twilio</SelectItem>
                  <SelectItem value="meta-cloud">meta-cloud</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.provider && <p className="text-sm text-destructive">{errors.provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="webhook_url">Webhook_url</Label>

          <Input id="webhook_url" type="text" {...register('webhook_url')} />
          {errors.webhook_url && <p className="text-sm text-destructive">{errors.webhook_url?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create WebhookConfig' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default WebhookConfigForm;
