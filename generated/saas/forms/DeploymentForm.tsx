// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { Deployment } from '../types/Deployment';

// ── Validation schema ──────────────────────────────────────────────────────
const deploymentFormSchema = z.object({
  solution_id: z.number().int().optional(),
  org_id: z.number().int().optional(),
  channel: z.enum(['slack', 'teams', 'whatsapp', 'telegram', 'desktop']).optional(),
  channel_config: z.string().optional(),
  status: z.enum(['active', 'paused', 'error', 'removed']).optional(),
  instance_id: z.string().optional(),
  health_status: z.enum(['healthy', 'degraded', 'down', 'unknown']).optional(),
  last_health_check: z.string().optional(),
});

export type DeploymentFormValues = z.infer<typeof deploymentFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface DeploymentFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Deployment>;
  onSubmit: (values: DeploymentFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function DeploymentForm({ mode, defaultValues, onSubmit, isLoading }: DeploymentFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<DeploymentFormValues>({
    resolver: zodResolver(deploymentFormSchema),
    defaultValues: defaultValues as DeploymentFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="solution_id">Solution_id</Label>

          <Input
            id="solution_id"
            type="number"
            {...register('solution_id', { valueAsNumber: true })}
          />
          {errors.solution_id && <p className="text-sm text-destructive">{errors.solution_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channel">Channel</Label>

          <Controller
            name="channel"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="channel">
                  <SelectValue placeholder="Select channel" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="slack">slack</SelectItem>
                  <SelectItem value="teams">teams</SelectItem>
                  <SelectItem value="whatsapp">whatsapp</SelectItem>
                  <SelectItem value="telegram">telegram</SelectItem>
                  <SelectItem value="desktop">desktop</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.channel && <p className="text-sm text-destructive">{errors.channel?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channel_config">Channel_config</Label>
          <p className="text-xs text-muted-foreground">Channel-specific config (bot token, webhook URL, etc.)</p>
          <Textarea id="channel_config" {...register('channel_config')} />
          {errors.channel_config && <p className="text-sm text-destructive">{errors.channel_config?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="paused">paused</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                  <SelectItem value="removed">removed</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="instance_id">Instance_id</Label>
          <p className="text-xs text-muted-foreground">Unique identifier for the running instance</p>
          <Input id="instance_id" type="text" {...register('instance_id')} />
          {errors.instance_id && <p className="text-sm text-destructive">{errors.instance_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="health_status">Health_status</Label>

          <Controller
            name="health_status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="health_status">
                  <SelectValue placeholder="Select health_status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="healthy">healthy</SelectItem>
                  <SelectItem value="degraded">degraded</SelectItem>
                  <SelectItem value="down">down</SelectItem>
                  <SelectItem value="unknown">unknown</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.health_status && <p className="text-sm text-destructive">{errors.health_status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_health_check">Last_health_check</Label>

          <Input id="last_health_check" type="text" {...register('last_health_check')} />
          {errors.last_health_check && <p className="text-sm text-destructive">{errors.last_health_check?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Deployment' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default DeploymentForm;
