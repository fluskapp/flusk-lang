// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';


import { Textarea } from '@/components/ui/textarea';
import type { AgentConfig } from '../types/AgentConfig';

// ── Validation schema ──────────────────────────────────────────────────────
const agentConfigFormSchema = z.object({
  org_id: z.number().int().optional(),
  version: z.number().int().optional(),
  otel_endpoint: z.string().optional(),
  heartbeat_interval_ms: z.number().int().optional(),
  approved_tools: z.string().optional(),
  intercept_patterns: z.string().optional(),
  solutions: z.string().optional(),
});

export type AgentConfigFormValues = z.infer<typeof agentConfigFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface AgentConfigFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<AgentConfig>;
  onSubmit: (values: AgentConfigFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function AgentConfigForm({ mode, defaultValues, onSubmit, isLoading }: AgentConfigFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<AgentConfigFormValues>({
    resolver: zodResolver(agentConfigFormSchema),
    defaultValues: defaultValues as AgentConfigFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="version">Version</Label>

          <Input
            id="version"
            type="number"
            {...register('version', { valueAsNumber: true })}
          />
          {errors.version && <p className="text-sm text-destructive">{errors.version?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="otel_endpoint">Otel_endpoint</Label>

          <Input id="otel_endpoint" type="text" {...register('otel_endpoint')} />
          {errors.otel_endpoint && <p className="text-sm text-destructive">{errors.otel_endpoint?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="heartbeat_interval_ms">Heartbeat_interval_ms</Label>

          <Input
            id="heartbeat_interval_ms"
            type="number"
            {...register('heartbeat_interval_ms', { valueAsNumber: true })}
          />
          {errors.heartbeat_interval_ms && <p className="text-sm text-destructive">{errors.heartbeat_interval_ms?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="approved_tools">Approved_tools</Label>
          <p className="text-xs text-muted-foreground">List of approved AI tool identifiers</p>
          <Textarea id="approved_tools" {...register('approved_tools')} />
          {errors.approved_tools && <p className="text-sm text-destructive">{errors.approved_tools?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="intercept_patterns">Intercept_patterns</Label>
          <p className="text-xs text-muted-foreground">URL patterns to intercept for OTel (e.g., api.openai.com, api.anthropic.com)</p>
          <Textarea id="intercept_patterns" {...register('intercept_patterns')} />
          {errors.intercept_patterns && <p className="text-sm text-destructive">{errors.intercept_patterns?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="solutions">Solutions</Label>
          <p className="text-xs text-muted-foreground">Active solution deployments for this org</p>
          <Textarea id="solutions" {...register('solutions')} />
          {errors.solutions && <p className="text-sm text-destructive">{errors.solutions?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create AgentConfig' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default AgentConfigForm;
