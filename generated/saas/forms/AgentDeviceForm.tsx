// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { AgentDevice } from '../types/AgentDevice';

// ── Validation schema ──────────────────────────────────────────────────────
const agentDeviceFormSchema = z.object({
  org_id: z.number().int().optional(),
  device_id: z.string().optional(),
  hostname: z.string().optional(),
  os: z.string().optional(),
  arch: z.string().optional(),
  agent_version: z.string().optional(),
  employee_email: z.string().optional(),
  last_seen_at: z.string().optional(),
  status: z.enum(['active', 'offline', 'revoked']).optional(),
});

export type AgentDeviceFormValues = z.infer<typeof agentDeviceFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface AgentDeviceFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<AgentDevice>;
  onSubmit: (values: AgentDeviceFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function AgentDeviceForm({ mode, defaultValues, onSubmit, isLoading }: AgentDeviceFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<AgentDeviceFormValues>({
    resolver: zodResolver(agentDeviceFormSchema),
    defaultValues: defaultValues as AgentDeviceFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="device_id">Device_id</Label>

          <Input id="device_id" type="text" {...register('device_id')} />
          {errors.device_id && <p className="text-sm text-destructive">{errors.device_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="hostname">Hostname</Label>

          <Input id="hostname" type="text" {...register('hostname')} />
          {errors.hostname && <p className="text-sm text-destructive">{errors.hostname?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="os">Os</Label>

          <Input id="os" type="text" {...register('os')} />
          {errors.os && <p className="text-sm text-destructive">{errors.os?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="arch">Arch</Label>

          <Input id="arch" type="text" {...register('arch')} />
          {errors.arch && <p className="text-sm text-destructive">{errors.arch?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="agent_version">Agent_version</Label>

          <Input id="agent_version" type="text" {...register('agent_version')} />
          {errors.agent_version && <p className="text-sm text-destructive">{errors.agent_version?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="employee_email">Employee_email</Label>

          <Input id="employee_email" type="text" {...register('employee_email')} />
          {errors.employee_email && <p className="text-sm text-destructive">{errors.employee_email?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_seen_at">Last_seen_at</Label>

          <Input id="last_seen_at" type="text" {...register('last_seen_at')} />
          {errors.last_seen_at && <p className="text-sm text-destructive">{errors.last_seen_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="offline">offline</SelectItem>
                  <SelectItem value="revoked">revoked</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create AgentDevice' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default AgentDeviceForm;
