// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { Conversation } from '../types/Conversation';

// ── Validation schema ──────────────────────────────────────────────────────
const conversationFormSchema = z.object({
  bot_id: z.string().optional(),
  contact_phone: z.string().optional(),
  contact_name: z.string().optional(),
  status: z.enum(['active', 'paused', 'archived']).optional(),
  last_message_at: z.string().optional(),
});

export type ConversationFormValues = z.infer<typeof conversationFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface ConversationFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Conversation>;
  onSubmit: (values: ConversationFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function ConversationForm({ mode, defaultValues, onSubmit, isLoading }: ConversationFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<ConversationFormValues>({
    resolver: zodResolver(conversationFormSchema),
    defaultValues: defaultValues as ConversationFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="bot_id">Bot_id</Label>

          <Input id="bot_id" type="text" {...register('bot_id')} />
          {errors.bot_id && <p className="text-sm text-destructive">{errors.bot_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="contact_phone">Contact_phone</Label>

          <Input id="contact_phone" type="text" {...register('contact_phone')} />
          {errors.contact_phone && <p className="text-sm text-destructive">{errors.contact_phone?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="contact_name">Contact_name</Label>

          <Input id="contact_name" type="text" {...register('contact_name')} />
          {errors.contact_name && <p className="text-sm text-destructive">{errors.contact_name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="paused">paused</SelectItem>
                  <SelectItem value="archived">archived</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_message_at">Last_message_at</Label>

          <Input id="last_message_at" type="text" {...register('last_message_at')} />
          {errors.last_message_at && <p className="text-sm text-destructive">{errors.last_message_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Conversation' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default ConversationForm;
