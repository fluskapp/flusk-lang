// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { BotMessage } from '../types/BotMessage';

// ── Validation schema ──────────────────────────────────────────────────────
const botMessageFormSchema = z.object({
  slack_ts: z.string().optional(),
  channel: z.string().optional(),
  text: z.string().optional(),
  status: z.enum(['pending', 'sent', 'failed']).optional(),
});

export type BotMessageFormValues = z.infer<typeof botMessageFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface BotMessageFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<BotMessage>;
  onSubmit: (values: BotMessageFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function BotMessageForm({ mode, defaultValues, onSubmit, isLoading }: BotMessageFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<BotMessageFormValues>({
    resolver: zodResolver(botMessageFormSchema),
    defaultValues: defaultValues as BotMessageFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="slack_ts">Slack_ts</Label>

          <Input id="slack_ts" type="text" {...register('slack_ts')} />
          {errors.slack_ts && <p className="text-sm text-destructive">{errors.slack_ts?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channel">Channel</Label>

          <Input id="channel" type="text" {...register('channel')} />
          {errors.channel && <p className="text-sm text-destructive">{errors.channel?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="text">Text</Label>

          <Input id="text" type="text" {...register('text')} />
          {errors.text && <p className="text-sm text-destructive">{errors.text?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="pending">pending</SelectItem>
                  <SelectItem value="sent">sent</SelectItem>
                  <SelectItem value="failed">failed</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create BotMessage' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default BotMessageForm;
