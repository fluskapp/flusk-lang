// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { Policy } from '../types/Policy';

// ── Validation schema ──────────────────────────────────────────────────────
const policyFormSchema = z.object({
  account_id: z.number().int().optional(),
  name: z.string().optional(),
  target_role: z.enum(['owner', 'admin', 'user', 'viewer']).optional(),
  target_department: z.string().optional(),
  rules: z.string().optional(),
  priority: z.number().int().optional(),
  status: z.enum(['active', 'draft', 'disabled']).optional(),
});

export type PolicyFormValues = z.infer<typeof policyFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface PolicyFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Policy>;
  onSubmit: (values: PolicyFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function PolicyForm({ mode, defaultValues, onSubmit, isLoading }: PolicyFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<PolicyFormValues>({
    resolver: zodResolver(policyFormSchema),
    defaultValues: defaultValues as PolicyFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="account_id">Account_id</Label>

          <Input
            id="account_id"
            type="number"
            {...register('account_id', { valueAsNumber: true })}
          />
          {errors.account_id && <p className="text-sm text-destructive">{errors.account_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="target_role">Target_role</Label>
          <p className="text-xs text-muted-foreground">Role this policy applies to; null means applies to all roles</p>
          <Controller
            name="target_role"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="target_role">
                  <SelectValue placeholder="Select target_role" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="owner">owner</SelectItem>
                  <SelectItem value="admin">admin</SelectItem>
                  <SelectItem value="user">user</SelectItem>
                  <SelectItem value="viewer">viewer</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.target_role && <p className="text-sm text-destructive">{errors.target_role?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="target_department">Target_department</Label>
          <p className="text-xs text-muted-foreground">Department this policy targets (org accounts only)</p>
          <Input id="target_department" type="text" {...register('target_department')} />
          {errors.target_department && <p className="text-sm text-destructive">{errors.target_department?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="rules">Rules</Label>
          <p className="text-xs text-muted-foreground">Policy rules: tools_allowed, tools_blocked, integrations_allowed, max_messages_per_day, code_execution, require_approval, content_filters, cost_limit_daily_cents, allowed_models, blocked_topics, data_classification</p>
          <Textarea id="rules" {...register('rules')} />
          {errors.rules && <p className="text-sm text-destructive">{errors.rules?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="priority">Priority</Label>
          <p className="text-xs text-muted-foreground">Higher priority policies override lower ones</p>
          <Input
            id="priority"
            type="number"
            {...register('priority', { valueAsNumber: true })}
          />
          {errors.priority && <p className="text-sm text-destructive">{errors.priority?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="draft">draft</SelectItem>
                  <SelectItem value="disabled">disabled</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Policy' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default PolicyForm;
