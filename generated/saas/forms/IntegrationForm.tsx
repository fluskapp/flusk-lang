// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { Integration } from '../types/Integration';

// ── Validation schema ──────────────────────────────────────────────────────
const integrationFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  provider: z.enum(['google', 'microsoft', 'notion', 'github', 'jira', 'slack', 'salesforce', 'hubspot', 'zapier']).optional(),
  scopes: z.string().optional(),
  access_token_ref: z.string().optional(),
  refresh_token_ref: z.string().optional(),
  token_expires_at: z.string().optional(),
  status: z.enum(['active', 'expired', 'revoked', 'error']).optional(),
  last_used_at: z.string().optional(),
});

export type IntegrationFormValues = z.infer<typeof integrationFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface IntegrationFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Integration>;
  onSubmit: (values: IntegrationFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function IntegrationForm({ mode, defaultValues, onSubmit, isLoading }: IntegrationFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<IntegrationFormValues>({
    resolver: zodResolver(integrationFormSchema),
    defaultValues: defaultValues as IntegrationFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="provider">Provider</Label>

          <Controller
            name="provider"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="provider">
                  <SelectValue placeholder="Select provider" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="google">google</SelectItem>
                  <SelectItem value="microsoft">microsoft</SelectItem>
                  <SelectItem value="notion">notion</SelectItem>
                  <SelectItem value="github">github</SelectItem>
                  <SelectItem value="jira">jira</SelectItem>
                  <SelectItem value="slack">slack</SelectItem>
                  <SelectItem value="salesforce">salesforce</SelectItem>
                  <SelectItem value="hubspot">hubspot</SelectItem>
                  <SelectItem value="zapier">zapier</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.provider && <p className="text-sm text-destructive">{errors.provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="scopes">Scopes</Label>
          <p className="text-xs text-muted-foreground">List of OAuth scopes granted</p>
          <Textarea id="scopes" {...register('scopes')} />
          {errors.scopes && <p className="text-sm text-destructive">{errors.scopes?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="access_token_ref">Access_token_ref</Label>
          <p className="text-xs text-muted-foreground">Reference key to secret vault entry for access token</p>
          <Input id="access_token_ref" type="text" {...register('access_token_ref')} />
          {errors.access_token_ref && <p className="text-sm text-destructive">{errors.access_token_ref?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="refresh_token_ref">Refresh_token_ref</Label>
          <p className="text-xs text-muted-foreground">Reference key to secret vault entry for refresh token</p>
          <Input id="refresh_token_ref" type="text" {...register('refresh_token_ref')} />
          {errors.refresh_token_ref && <p className="text-sm text-destructive">{errors.refresh_token_ref?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="token_expires_at">Token_expires_at</Label>

          <Input id="token_expires_at" type="text" {...register('token_expires_at')} />
          {errors.token_expires_at && <p className="text-sm text-destructive">{errors.token_expires_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="expired">expired</SelectItem>
                  <SelectItem value="revoked">revoked</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_used_at">Last_used_at</Label>

          <Input id="last_used_at" type="text" {...register('last_used_at')} />
          {errors.last_used_at && <p className="text-sm text-destructive">{errors.last_used_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Integration' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default IntegrationForm;
