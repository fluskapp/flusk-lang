// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';

import { Switch } from '@/components/ui/switch';
import { Textarea } from '@/components/ui/textarea';
import type { ProxyConfig } from '../types/ProxyConfig';

// ── Validation schema ──────────────────────────────────────────────────────
const proxyConfigFormSchema = z.object({
  bot_id: z.number().int().optional(),
  pii_detection: z.boolean().optional(),
  pii_masking: z.boolean().optional(),
  cost_cap_daily_usd: z.number().optional(),
  cost_cap_monthly_usd: z.number().optional(),
  rate_limit_per_minute: z.number().int().optional(),
  rate_limit_per_day: z.number().int().optional(),
  allowed_tools: z.string().optional(),
  blocked_tools: z.string().optional(),
  allowed_domains: z.string().optional(),
  require_approval: z.boolean().optional(),
  intent_validation: z.boolean().optional(),
});

export type ProxyConfigFormValues = z.infer<typeof proxyConfigFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface ProxyConfigFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<ProxyConfig>;
  onSubmit: (values: ProxyConfigFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function ProxyConfigForm({ mode, defaultValues, onSubmit, isLoading }: ProxyConfigFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<ProxyConfigFormValues>({
    resolver: zodResolver(proxyConfigFormSchema),
    defaultValues: defaultValues as ProxyConfigFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="bot_id">Bot_id</Label>

          <Input
            id="bot_id"
            type="number"
            {...register('bot_id', { valueAsNumber: true })}
          />
          {errors.bot_id && <p className="text-sm text-destructive">{errors.bot_id?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="pii_detection">Pii_detection</Label>

          </div>
          <Controller
            name="pii_detection"
            control={control}
            render={({ field: f }) => (
              <Switch id="pii_detection" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.pii_detection && <p className="text-sm text-destructive">{errors.pii_detection?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="pii_masking">Pii_masking</Label>
          <p className="text-xs text-muted-foreground">Whether to mask PII in outbound messages</p>
          </div>
          <Controller
            name="pii_masking"
            control={control}
            render={({ field: f }) => (
              <Switch id="pii_masking" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.pii_masking && <p className="text-sm text-destructive">{errors.pii_masking?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_cap_daily_usd">Cost_cap_daily_usd</Label>
          <p className="text-xs text-muted-foreground">Daily cost cap in USD (null = unlimited)</p>
          <Input
            id="cost_cap_daily_usd"
            type="number"
            {...register('cost_cap_daily_usd', { valueAsNumber: true })}
          />
          {errors.cost_cap_daily_usd && <p className="text-sm text-destructive">{errors.cost_cap_daily_usd?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_cap_monthly_usd">Cost_cap_monthly_usd</Label>

          <Input
            id="cost_cap_monthly_usd"
            type="number"
            {...register('cost_cap_monthly_usd', { valueAsNumber: true })}
          />
          {errors.cost_cap_monthly_usd && <p className="text-sm text-destructive">{errors.cost_cap_monthly_usd?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="rate_limit_per_minute">Rate_limit_per_minute</Label>

          <Input
            id="rate_limit_per_minute"
            type="number"
            {...register('rate_limit_per_minute', { valueAsNumber: true })}
          />
          {errors.rate_limit_per_minute && <p className="text-sm text-destructive">{errors.rate_limit_per_minute?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="rate_limit_per_day">Rate_limit_per_day</Label>

          <Input
            id="rate_limit_per_day"
            type="number"
            {...register('rate_limit_per_day', { valueAsNumber: true })}
          />
          {errors.rate_limit_per_day && <p className="text-sm text-destructive">{errors.rate_limit_per_day?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="allowed_tools">Allowed_tools</Label>
          <p className="text-xs text-muted-foreground">List of allowed tool names (null = all allowed)</p>
          <Textarea id="allowed_tools" {...register('allowed_tools')} />
          {errors.allowed_tools && <p className="text-sm text-destructive">{errors.allowed_tools?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="blocked_tools">Blocked_tools</Label>
          <p className="text-xs text-muted-foreground">List of blocked tool names</p>
          <Textarea id="blocked_tools" {...register('blocked_tools')} />
          {errors.blocked_tools && <p className="text-sm text-destructive">{errors.blocked_tools?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="allowed_domains">Allowed_domains</Label>
          <p className="text-xs text-muted-foreground">Allowed outbound domains</p>
          <Textarea id="allowed_domains" {...register('allowed_domains')} />
          {errors.allowed_domains && <p className="text-sm text-destructive">{errors.allowed_domains?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="require_approval">Require_approval</Label>
          <p className="text-xs text-muted-foreground">Enterprise - require human approval for risky actions</p>
          </div>
          <Controller
            name="require_approval"
            control={control}
            render={({ field: f }) => (
              <Switch id="require_approval" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.require_approval && <p className="text-sm text-destructive">{errors.require_approval?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="intent_validation">Intent_validation</Label>
          <p className="text-xs text-muted-foreground">Validate assistant actions match user intent</p>
          </div>
          <Controller
            name="intent_validation"
            control={control}
            render={({ field: f }) => (
              <Switch id="intent_validation" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.intent_validation && <p className="text-sm text-destructive">{errors.intent_validation?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create ProxyConfig' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default ProxyConfigForm;
