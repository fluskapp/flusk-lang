// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';

import type { AiTool } from '../types/AiTool';

// ── Validation schema ──────────────────────────────────────────────────────
const aiToolFormSchema = z.object({
  org_id: z.number().int().optional(),
  name: z.string().optional(),
  provider: z.string().optional(),
  category: z.enum(['chat', 'coding', 'image', 'voice', 'search', 'other']).optional(),
  approved: z.boolean().optional(),
  detection_pattern: z.string().optional(),
  first_seen_at: z.string().optional(),
  last_seen_at: z.string().optional(),
});

export type AiToolFormValues = z.infer<typeof aiToolFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface AiToolFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<AiTool>;
  onSubmit: (values: AiToolFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function AiToolForm({ mode, defaultValues, onSubmit, isLoading }: AiToolFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<AiToolFormValues>({
    resolver: zodResolver(aiToolFormSchema),
    defaultValues: defaultValues as AiToolFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="provider">Provider</Label>

          <Input id="provider" type="text" {...register('provider')} />
          {errors.provider && <p className="text-sm text-destructive">{errors.provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="category">Category</Label>

          <Controller
            name="category"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="category">
                  <SelectValue placeholder="Select category" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="chat">chat</SelectItem>
                  <SelectItem value="coding">coding</SelectItem>
                  <SelectItem value="image">image</SelectItem>
                  <SelectItem value="voice">voice</SelectItem>
                  <SelectItem value="search">search</SelectItem>
                  <SelectItem value="other">other</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.category && <p className="text-sm text-destructive">{errors.category?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="approved">Approved</Label>

          </div>
          <Controller
            name="approved"
            control={control}
            render={({ field: f }) => (
              <Switch id="approved" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.approved && <p className="text-sm text-destructive">{errors.approved?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="detection_pattern">Detection_pattern</Label>

          <Input id="detection_pattern" type="text" {...register('detection_pattern')} />
          {errors.detection_pattern && <p className="text-sm text-destructive">{errors.detection_pattern?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="first_seen_at">First_seen_at</Label>

          <Input id="first_seen_at" type="text" {...register('first_seen_at')} />
          {errors.first_seen_at && <p className="text-sm text-destructive">{errors.first_seen_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_seen_at">Last_seen_at</Label>

          <Input id="last_seen_at" type="text" {...register('last_seen_at')} />
          {errors.last_seen_at && <p className="text-sm text-destructive">{errors.last_seen_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create AiTool' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default AiToolForm;
