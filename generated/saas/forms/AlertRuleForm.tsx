// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
import { Switch } from '@/components/ui/switch';
import { Textarea } from '@/components/ui/textarea';
import type { AlertRule } from '../types/AlertRule';

// ── Validation schema ──────────────────────────────────────────────────────
const alertRuleFormSchema = z.object({
  org_id: z.number().int().optional(),
  name: z.string().optional(),
  type: z.enum(['shadow-ai', 'usage-spike', 'new-tool', 'budget-exceeded', 'device-offline']).optional(),
  threshold: z.string().optional(),
  channels: z.string().optional(),
  enabled: z.boolean().optional(),
});

export type AlertRuleFormValues = z.infer<typeof alertRuleFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface AlertRuleFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<AlertRule>;
  onSubmit: (values: AlertRuleFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function AlertRuleForm({ mode, defaultValues, onSubmit, isLoading }: AlertRuleFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<AlertRuleFormValues>({
    resolver: zodResolver(alertRuleFormSchema),
    defaultValues: defaultValues as AlertRuleFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="type">Type</Label>

          <Controller
            name="type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="type">
                  <SelectValue placeholder="Select type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="shadow-ai">shadow-ai</SelectItem>
                  <SelectItem value="usage-spike">usage-spike</SelectItem>
                  <SelectItem value="new-tool">new-tool</SelectItem>
                  <SelectItem value="budget-exceeded">budget-exceeded</SelectItem>
                  <SelectItem value="device-offline">device-offline</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.type && <p className="text-sm text-destructive">{errors.type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="threshold">Threshold</Label>

          <Textarea id="threshold" {...register('threshold')} />
          {errors.threshold && <p className="text-sm text-destructive">{errors.threshold?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channels">Channels</Label>
          <p className="text-xs text-muted-foreground">Array of notification channels (email, slack, webhook)</p>
          <Textarea id="channels" {...register('channels')} />
          {errors.channels && <p className="text-sm text-destructive">{errors.channels?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="enabled">Enabled</Label>

          </div>
          <Controller
            name="enabled"
            control={control}
            render={({ field: f }) => (
              <Switch id="enabled" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.enabled && <p className="text-sm text-destructive">{errors.enabled?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create AlertRule' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default AlertRuleForm;
