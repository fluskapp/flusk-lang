// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { Account } from '../types/Account';

// ── Validation schema ──────────────────────────────────────────────────────
const accountFormSchema = z.object({
  type: z.enum(['personal', 'organization']).optional(),
  name: z.string().optional(),
  slug: z.string().optional(),
  owner_email: z.string().optional(),
  plan: z.enum(['free', 'pro_starter', 'pro', 'pro_power', 'enterprise']).optional(),
  llm_mode: z.enum(['managed', 'byok']).optional(),
  llm_budget_cents: z.number().int().optional(),
  status: z.enum(['active', 'suspended', 'trial', 'cancelled']).optional(),
  trial_ends_at: z.string().optional(),
  stripe_customer_id: z.string().optional(),
  stripe_subscription_id: z.string().optional(),
  sso_provider: z.enum(['google', 'okta', 'azure_ad']).optional(),
  sso_config: z.string().optional(),
  max_assistants: z.number().int().optional(),
  max_users: z.number().int().optional(),
});

export type AccountFormValues = z.infer<typeof accountFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface AccountFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Account>;
  onSubmit: (values: AccountFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function AccountForm({ mode, defaultValues, onSubmit, isLoading }: AccountFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<AccountFormValues>({
    resolver: zodResolver(accountFormSchema),
    defaultValues: defaultValues as AccountFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="type">Type</Label>

          <Controller
            name="type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="type">
                  <SelectValue placeholder="Select type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="personal">personal</SelectItem>
                  <SelectItem value="organization">organization</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.type && <p className="text-sm text-destructive">{errors.type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="slug">Slug</Label>

          <Input id="slug" type="text" {...register('slug')} />
          {errors.slug && <p className="text-sm text-destructive">{errors.slug?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="owner_email">Owner_email</Label>

          <Input id="owner_email" type="text" {...register('owner_email')} />
          {errors.owner_email && <p className="text-sm text-destructive">{errors.owner_email?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="plan">Plan</Label>

          <Controller
            name="plan"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="plan">
                  <SelectValue placeholder="Select plan" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="free">free</SelectItem>
                  <SelectItem value="pro_starter">pro_starter</SelectItem>
                  <SelectItem value="pro">pro</SelectItem>
                  <SelectItem value="pro_power">pro_power</SelectItem>
                  <SelectItem value="enterprise">enterprise</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.plan && <p className="text-sm text-destructive">{errors.plan?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="llm_mode">Llm_mode</Label>
          <p className="text-xs text-muted-foreground">managed = Flusk provides LLM, byok = bring your own key</p>
          <Controller
            name="llm_mode"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="llm_mode">
                  <SelectValue placeholder="Select llm_mode" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="managed">managed</SelectItem>
                  <SelectItem value="byok">byok</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.llm_mode && <p className="text-sm text-destructive">{errors.llm_mode?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="llm_budget_cents">Llm_budget_cents</Label>
          <p className="text-xs text-muted-foreground">Monthly LLM spend cap in cents</p>
          <Input
            id="llm_budget_cents"
            type="number"
            {...register('llm_budget_cents', { valueAsNumber: true })}
          />
          {errors.llm_budget_cents && <p className="text-sm text-destructive">{errors.llm_budget_cents?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="suspended">suspended</SelectItem>
                  <SelectItem value="trial">trial</SelectItem>
                  <SelectItem value="cancelled">cancelled</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="trial_ends_at">Trial_ends_at</Label>

          <Input id="trial_ends_at" type="text" {...register('trial_ends_at')} />
          {errors.trial_ends_at && <p className="text-sm text-destructive">{errors.trial_ends_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="stripe_customer_id">Stripe_customer_id</Label>

          <Input id="stripe_customer_id" type="text" {...register('stripe_customer_id')} />
          {errors.stripe_customer_id && <p className="text-sm text-destructive">{errors.stripe_customer_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="stripe_subscription_id">Stripe_subscription_id</Label>

          <Input id="stripe_subscription_id" type="text" {...register('stripe_subscription_id')} />
          {errors.stripe_subscription_id && <p className="text-sm text-destructive">{errors.stripe_subscription_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="sso_provider">Sso_provider</Label>
          <p className="text-xs text-muted-foreground">Organization accounts only</p>
          <Controller
            name="sso_provider"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="sso_provider">
                  <SelectValue placeholder="Select sso_provider" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="google">google</SelectItem>
                  <SelectItem value="okta">okta</SelectItem>
                  <SelectItem value="azure_ad">azure_ad</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.sso_provider && <p className="text-sm text-destructive">{errors.sso_provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="sso_config">Sso_config</Label>
          <p className="text-xs text-muted-foreground">SSO provider-specific configuration</p>
          <Textarea id="sso_config" {...register('sso_config')} />
          {errors.sso_config && <p className="text-sm text-destructive">{errors.sso_config?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="max_assistants">Max_assistants</Label>

          <Input
            id="max_assistants"
            type="number"
            {...register('max_assistants', { valueAsNumber: true })}
          />
          {errors.max_assistants && <p className="text-sm text-destructive">{errors.max_assistants?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="max_users">Max_users</Label>

          <Input
            id="max_users"
            type="number"
            {...register('max_users', { valueAsNumber: true })}
          />
          {errors.max_users && <p className="text-sm text-destructive">{errors.max_users?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Account' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default AccountForm;
