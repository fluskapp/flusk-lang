// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { DaySummary } from '../types/DaySummary';

// ── Validation schema ──────────────────────────────────────────────────────
const daySummaryFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  date: z.string().optional(),
  summary_text: z.string().optional(),
  key_decisions: z.string().optional(),
  goals_progress: z.string().optional(),
  people_mentioned: z.string().optional(),
  personality_notes: z.string().optional(),
  things_to_remember: z.string().optional(),
  message_count: z.number().int().optional(),
  action_count: z.number().int().optional(),
  cost_cents: z.number().int().optional(),
  guard_blocks: z.number().int().optional(),
  pii_detections: z.number().int().optional(),
  channels_used: z.string().optional(),
  mood: z.enum(['productive', 'creative', 'busy', 'quiet', 'stressed', 'exploratory']).optional(),
});

export type DaySummaryFormValues = z.infer<typeof daySummaryFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface DaySummaryFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<DaySummary>;
  onSubmit: (values: DaySummaryFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function DaySummaryForm({ mode, defaultValues, onSubmit, isLoading }: DaySummaryFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<DaySummaryFormValues>({
    resolver: zodResolver(daySummaryFormSchema),
    defaultValues: defaultValues as DaySummaryFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="date">Date</Label>

          <Input id="date" type="text" {...register('date')} />
          {errors.date && <p className="text-sm text-destructive">{errors.date?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="summary_text">Summary_text</Label>
          <p className="text-xs text-muted-foreground">Human-readable narrative of the day (markdown, with emoji, written like a diary entry)</p>
          <Input id="summary_text" type="text" {...register('summary_text')} />
          {errors.summary_text && <p className="text-sm text-destructive">{errors.summary_text?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="key_decisions">Key_decisions</Label>
          <p className="text-xs text-muted-foreground">Array of important decisions made today</p>
          <Textarea id="key_decisions" {...register('key_decisions')} />
          {errors.key_decisions && <p className="text-sm text-destructive">{errors.key_decisions?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="goals_progress">Goals_progress</Label>
          <p className="text-xs text-muted-foreground">Array of goals and their status updates</p>
          <Textarea id="goals_progress" {...register('goals_progress')} />
          {errors.goals_progress && <p className="text-sm text-destructive">{errors.goals_progress?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="people_mentioned">People_mentioned</Label>
          <p className="text-xs text-muted-foreground">Array of people mentioned in conversations</p>
          <Textarea id="people_mentioned" {...register('people_mentioned')} />
          {errors.people_mentioned && <p className="text-sm text-destructive">{errors.people_mentioned?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="personality_notes">Personality_notes</Label>
          <p className="text-xs text-muted-foreground">Things learned about the user's preferences</p>
          <Textarea id="personality_notes" {...register('personality_notes')} />
          {errors.personality_notes && <p className="text-sm text-destructive">{errors.personality_notes?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="things_to_remember">Things_to_remember</Label>
          <p className="text-xs text-muted-foreground">Items flagged for long-term memory</p>
          <Textarea id="things_to_remember" {...register('things_to_remember')} />
          {errors.things_to_remember && <p className="text-sm text-destructive">{errors.things_to_remember?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="message_count">Message_count</Label>

          <Input
            id="message_count"
            type="number"
            {...register('message_count', { valueAsNumber: true })}
          />
          {errors.message_count && <p className="text-sm text-destructive">{errors.message_count?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="action_count">Action_count</Label>

          <Input
            id="action_count"
            type="number"
            {...register('action_count', { valueAsNumber: true })}
          />
          {errors.action_count && <p className="text-sm text-destructive">{errors.action_count?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_cents">Cost_cents</Label>

          <Input
            id="cost_cents"
            type="number"
            {...register('cost_cents', { valueAsNumber: true })}
          />
          {errors.cost_cents && <p className="text-sm text-destructive">{errors.cost_cents?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="guard_blocks">Guard_blocks</Label>

          <Input
            id="guard_blocks"
            type="number"
            {...register('guard_blocks', { valueAsNumber: true })}
          />
          {errors.guard_blocks && <p className="text-sm text-destructive">{errors.guard_blocks?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="pii_detections">Pii_detections</Label>

          <Input
            id="pii_detections"
            type="number"
            {...register('pii_detections', { valueAsNumber: true })}
          />
          {errors.pii_detections && <p className="text-sm text-destructive">{errors.pii_detections?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channels_used">Channels_used</Label>
          <p className="text-xs text-muted-foreground">Array of channel names used today</p>
          <Textarea id="channels_used" {...register('channels_used')} />
          {errors.channels_used && <p className="text-sm text-destructive">{errors.channels_used?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="mood">Mood</Label>
          <p className="text-xs text-muted-foreground">Overall vibe of the day (determined by summary generator)</p>
          <Controller
            name="mood"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="mood">
                  <SelectValue placeholder="Select mood" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="productive">productive</SelectItem>
                  <SelectItem value="creative">creative</SelectItem>
                  <SelectItem value="busy">busy</SelectItem>
                  <SelectItem value="quiet">quiet</SelectItem>
                  <SelectItem value="stressed">stressed</SelectItem>
                  <SelectItem value="exploratory">exploratory</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.mood && <p className="text-sm text-destructive">{errors.mood?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create DaySummary' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default DaySummaryForm;
