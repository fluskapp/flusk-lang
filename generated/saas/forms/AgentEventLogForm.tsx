// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { AgentEventLog } from '../types/AgentEventLog';

// ── Validation schema ──────────────────────────────────────────────────────
const agentEventLogFormSchema = z.object({
  org_id: z.number().int().optional(),
  device_id: z.string().optional(),
  event_type: z.enum(['connected', 'disconnected', 'config-updated', 'error', 'solution-received']).optional(),
  details: z.string().optional(),
});

export type AgentEventLogFormValues = z.infer<typeof agentEventLogFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface AgentEventLogFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<AgentEventLog>;
  onSubmit: (values: AgentEventLogFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function AgentEventLogForm({ mode, defaultValues, onSubmit, isLoading }: AgentEventLogFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<AgentEventLogFormValues>({
    resolver: zodResolver(agentEventLogFormSchema),
    defaultValues: defaultValues as AgentEventLogFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="device_id">Device_id</Label>

          <Input id="device_id" type="text" {...register('device_id')} />
          {errors.device_id && <p className="text-sm text-destructive">{errors.device_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="event_type">Event_type</Label>

          <Controller
            name="event_type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="event_type">
                  <SelectValue placeholder="Select event_type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="connected">connected</SelectItem>
                  <SelectItem value="disconnected">disconnected</SelectItem>
                  <SelectItem value="config-updated">config-updated</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                  <SelectItem value="solution-received">solution-received</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.event_type && <p className="text-sm text-destructive">{errors.event_type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="details">Details</Label>

          <Textarea id="details" {...register('details')} />
          {errors.details && <p className="text-sm text-destructive">{errors.details?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create AgentEventLog' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default AgentEventLogForm;
