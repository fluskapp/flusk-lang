// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { Solution } from '../types/Solution';

// ── Validation schema ──────────────────────────────────────────────────────
const solutionFormSchema = z.object({
  org_id: z.number().int().optional(),
  name: z.string().optional(),
  description: z.string().optional(),
  type: z.enum(['openclaw', 'custom']).optional(),
  llm_provider: z.enum(['openai', 'anthropic', 'google', 'azure-openai', 'custom']).optional(),
  llm_model: z.string().optional(),
  system_prompt: z.string().optional(),
  temperature: z.number().optional(),
  max_tokens: z.number().int().optional(),
  tools_config: z.string().optional(),
  status: z.enum(['draft', 'testing', 'published', 'archived']).optional(),
  created_by: z.number().int().optional(),
  published_at: z.string().optional(),
});

export type SolutionFormValues = z.infer<typeof solutionFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface SolutionFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Solution>;
  onSubmit: (values: SolutionFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function SolutionForm({ mode, defaultValues, onSubmit, isLoading }: SolutionFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<SolutionFormValues>({
    resolver: zodResolver(solutionFormSchema),
    defaultValues: defaultValues as SolutionFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="name">Name</Label>

          <Input id="name" type="text" {...register('name')} />
          {errors.name && <p className="text-sm text-destructive">{errors.name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="description">Description</Label>

          <Input id="description" type="text" {...register('description')} />
          {errors.description && <p className="text-sm text-destructive">{errors.description?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="type">Type</Label>

          <Controller
            name="type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="type">
                  <SelectValue placeholder="Select type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="openclaw">openclaw</SelectItem>
                  <SelectItem value="custom">custom</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.type && <p className="text-sm text-destructive">{errors.type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="llm_provider">Llm_provider</Label>

          <Controller
            name="llm_provider"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="llm_provider">
                  <SelectValue placeholder="Select llm_provider" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="openai">openai</SelectItem>
                  <SelectItem value="anthropic">anthropic</SelectItem>
                  <SelectItem value="google">google</SelectItem>
                  <SelectItem value="azure-openai">azure-openai</SelectItem>
                  <SelectItem value="custom">custom</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.llm_provider && <p className="text-sm text-destructive">{errors.llm_provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="llm_model">Llm_model</Label>

          <Input id="llm_model" type="text" {...register('llm_model')} />
          {errors.llm_model && <p className="text-sm text-destructive">{errors.llm_model?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="system_prompt">System_prompt</Label>

          <Input id="system_prompt" type="text" {...register('system_prompt')} />
          {errors.system_prompt && <p className="text-sm text-destructive">{errors.system_prompt?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="temperature">Temperature</Label>

          <Input
            id="temperature"
            type="number"
            {...register('temperature', { valueAsNumber: true })}
          />
          {errors.temperature && <p className="text-sm text-destructive">{errors.temperature?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="max_tokens">Max_tokens</Label>

          <Input
            id="max_tokens"
            type="number"
            {...register('max_tokens', { valueAsNumber: true })}
          />
          {errors.max_tokens && <p className="text-sm text-destructive">{errors.max_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="tools_config">Tools_config</Label>
          <p className="text-xs text-muted-foreground">Tool/skill configuration for the AI agent</p>
          <Textarea id="tools_config" {...register('tools_config')} />
          {errors.tools_config && <p className="text-sm text-destructive">{errors.tools_config?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="draft">draft</SelectItem>
                  <SelectItem value="testing">testing</SelectItem>
                  <SelectItem value="published">published</SelectItem>
                  <SelectItem value="archived">archived</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="created_by">Created_by</Label>

          <Input
            id="created_by"
            type="number"
            {...register('created_by', { valueAsNumber: true })}
          />
          {errors.created_by && <p className="text-sm text-destructive">{errors.created_by?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="published_at">Published_at</Label>

          <Input id="published_at" type="text" {...register('published_at')} />
          {errors.published_at && <p className="text-sm text-destructive">{errors.published_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Solution' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default SolutionForm;
