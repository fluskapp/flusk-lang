// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';

import { Textarea } from '@/components/ui/textarea';
import type { Channel } from '../types/Channel';

// ── Validation schema ──────────────────────────────────────────────────────
const channelFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  type: z.enum(['whatsapp', 'telegram', 'slack', 'discord', 'web', 'email']).optional(),
  status: z.enum(['pending', 'connecting', 'active', 'disconnected', 'error']).optional(),
  config: z.string().optional(),
  external_id: z.string().optional(),
  last_message_at: z.string().optional(),
  message_count: z.number().int().optional(),
});

export type ChannelFormValues = z.infer<typeof channelFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface ChannelFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<Channel>;
  onSubmit: (values: ChannelFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function ChannelForm({ mode, defaultValues, onSubmit, isLoading }: ChannelFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<ChannelFormValues>({
    resolver: zodResolver(channelFormSchema),
    defaultValues: defaultValues as ChannelFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="type">Type</Label>

          <Controller
            name="type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="type">
                  <SelectValue placeholder="Select type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="whatsapp">whatsapp</SelectItem>
                  <SelectItem value="telegram">telegram</SelectItem>
                  <SelectItem value="slack">slack</SelectItem>
                  <SelectItem value="discord">discord</SelectItem>
                  <SelectItem value="web">web</SelectItem>
                  <SelectItem value="email">email</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.type && <p className="text-sm text-destructive">{errors.type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="pending">pending</SelectItem>
                  <SelectItem value="connecting">connecting</SelectItem>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="disconnected">disconnected</SelectItem>
                  <SelectItem value="error">error</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="config">Config</Label>
          <p className="text-xs text-muted-foreground">Channel-specific config (phone number, bot token, webhook URL, etc.)</p>
          <Textarea id="config" {...register('config')} />
          {errors.config && <p className="text-sm text-destructive">{errors.config?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="external_id">External_id</Label>
          <p className="text-xs text-muted-foreground">External platform identifier (channel ID, phone number, etc.)</p>
          <Input id="external_id" type="text" {...register('external_id')} />
          {errors.external_id && <p className="text-sm text-destructive">{errors.external_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_message_at">Last_message_at</Label>

          <Input id="last_message_at" type="text" {...register('last_message_at')} />
          {errors.last_message_at && <p className="text-sm text-destructive">{errors.last_message_at?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="message_count">Message_count</Label>

          <Input
            id="message_count"
            type="number"
            {...register('message_count', { valueAsNumber: true })}
          />
          {errors.message_count && <p className="text-sm text-destructive">{errors.message_count?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create Channel' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default ChannelForm;
