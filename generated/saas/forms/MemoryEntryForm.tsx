// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { MemoryEntry } from '../types/MemoryEntry';

// ── Validation schema ──────────────────────────────────────────────────────
const memoryEntryFormSchema = z.object({
  assistant_id: z.number().int().optional(),
  category: z.enum(['decision', 'preference', 'goal', 'person', 'date', 'boundary', 'insight']).optional(),
  content: z.string().optional(),
  context: z.string().optional(),
  source_date: z.string().optional(),
  last_referenced: z.string().optional(),
  importance: z.enum(['critical', 'high', 'normal', 'low']).optional(),
  status: z.enum(['active', 'archived', 'expired']).optional(),
});

export type MemoryEntryFormValues = z.infer<typeof memoryEntryFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface MemoryEntryFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<MemoryEntry>;
  onSubmit: (values: MemoryEntryFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function MemoryEntryForm({ mode, defaultValues, onSubmit, isLoading }: MemoryEntryFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<MemoryEntryFormValues>({
    resolver: zodResolver(memoryEntryFormSchema),
    defaultValues: defaultValues as MemoryEntryFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="assistant_id">Assistant_id</Label>

          <Input
            id="assistant_id"
            type="number"
            {...register('assistant_id', { valueAsNumber: true })}
          />
          {errors.assistant_id && <p className="text-sm text-destructive">{errors.assistant_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="category">Category</Label>
          <p className="text-xs text-muted-foreground">What kind of memory this is</p>
          <Controller
            name="category"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="category">
                  <SelectValue placeholder="Select category" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="decision">decision</SelectItem>
                  <SelectItem value="preference">preference</SelectItem>
                  <SelectItem value="goal">goal</SelectItem>
                  <SelectItem value="person">person</SelectItem>
                  <SelectItem value="date">date</SelectItem>
                  <SelectItem value="boundary">boundary</SelectItem>
                  <SelectItem value="insight">insight</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.category && <p className="text-sm text-destructive">{errors.category?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="content">Content</Label>
          <p className="text-xs text-muted-foreground">The actual memory in human-readable form</p>
          <Input id="content" type="text" {...register('content')} />
          {errors.content && <p className="text-sm text-destructive">{errors.content?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="context">Context</Label>
          <p className="text-xs text-muted-foreground">Why this matters or when it came up</p>
          <Input id="context" type="text" {...register('context')} />
          {errors.context && <p className="text-sm text-destructive">{errors.context?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="source_date">Source_date</Label>
          <p className="text-xs text-muted-foreground">When this was first learned</p>
          <Input id="source_date" type="text" {...register('source_date')} />
          {errors.source_date && <p className="text-sm text-destructive">{errors.source_date?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_referenced">Last_referenced</Label>
          <p className="text-xs text-muted-foreground">Last time this memory was relevant in a conversation</p>
          <Input id="last_referenced" type="text" {...register('last_referenced')} />
          {errors.last_referenced && <p className="text-sm text-destructive">{errors.last_referenced?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="importance">Importance</Label>
          <p className="text-xs text-muted-foreground">How important this is to keep</p>
          <Controller
            name="importance"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="importance">
                  <SelectValue placeholder="Select importance" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="critical">critical</SelectItem>
                  <SelectItem value="high">high</SelectItem>
                  <SelectItem value="normal">normal</SelectItem>
                  <SelectItem value="low">low</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.importance && <p className="text-sm text-destructive">{errors.importance?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>
          <p className="text-xs text-muted-foreground">Active memories are in MEMORY.md, archived are kept but not loaded</p>
          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="archived">archived</SelectItem>
                  <SelectItem value="expired">expired</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create MemoryEntry' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default MemoryEntryForm;
