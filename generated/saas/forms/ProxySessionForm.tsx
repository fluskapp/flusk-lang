// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';


import type { ProxySession } from '../types/ProxySession';

// ── Validation schema ──────────────────────────────────────────────────────
const proxySessionFormSchema = z.object({
  bot_id: z.number().int().optional(),
  session_key: z.string().optional(),
  channel: z.string().optional(),
  chat_type: z.enum(['direct', 'group']).optional(),
  display_name: z.string().optional(),
  model: z.string().optional(),
  total_tokens: z.number().int().optional(),
  total_cost_usd: z.number().optional(),
  message_count: z.number().int().optional(),
  last_activity: z.string().optional(),
  status: z.enum(['active', 'idle', 'closed']).optional(),
});

export type ProxySessionFormValues = z.infer<typeof proxySessionFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface ProxySessionFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<ProxySession>;
  onSubmit: (values: ProxySessionFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function ProxySessionForm({ mode, defaultValues, onSubmit, isLoading }: ProxySessionFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<ProxySessionFormValues>({
    resolver: zodResolver(proxySessionFormSchema),
    defaultValues: defaultValues as ProxySessionFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="bot_id">Bot_id</Label>

          <Input
            id="bot_id"
            type="number"
            {...register('bot_id', { valueAsNumber: true })}
          />
          {errors.bot_id && <p className="text-sm text-destructive">{errors.bot_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="session_key">Session_key</Label>
          <p className="text-xs text-muted-foreground">Unique session identifier from the runtime</p>
          <Input id="session_key" type="text" {...register('session_key')} />
          {errors.session_key && <p className="text-sm text-destructive">{errors.session_key?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="channel">Channel</Label>
          <p className="text-xs text-muted-foreground">whatsapp, telegram, web, api</p>
          <Input id="channel" type="text" {...register('channel')} />
          {errors.channel && <p className="text-sm text-destructive">{errors.channel?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="chat_type">Chat_type</Label>

          <Controller
            name="chat_type"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="chat_type">
                  <SelectValue placeholder="Select chat_type" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="direct">direct</SelectItem>
                  <SelectItem value="group">group</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.chat_type && <p className="text-sm text-destructive">{errors.chat_type?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="display_name">Display_name</Label>
          <p className="text-xs text-muted-foreground">Human-readable session label (e.g. "WhatsApp DM")</p>
          <Input id="display_name" type="text" {...register('display_name')} />
          {errors.display_name && <p className="text-sm text-destructive">{errors.display_name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="model">Model</Label>

          <Input id="model" type="text" {...register('model')} />
          {errors.model && <p className="text-sm text-destructive">{errors.model?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="total_tokens">Total_tokens</Label>

          <Input
            id="total_tokens"
            type="number"
            {...register('total_tokens', { valueAsNumber: true })}
          />
          {errors.total_tokens && <p className="text-sm text-destructive">{errors.total_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="total_cost_usd">Total_cost_usd</Label>

          <Input
            id="total_cost_usd"
            type="number"
            {...register('total_cost_usd', { valueAsNumber: true })}
          />
          {errors.total_cost_usd && <p className="text-sm text-destructive">{errors.total_cost_usd?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="message_count">Message_count</Label>

          <Input
            id="message_count"
            type="number"
            {...register('message_count', { valueAsNumber: true })}
          />
          {errors.message_count && <p className="text-sm text-destructive">{errors.message_count?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_activity">Last_activity</Label>

          <Input id="last_activity" type="text" {...register('last_activity')} />
          {errors.last_activity && <p className="text-sm text-destructive">{errors.last_activity?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status">Status</Label>

          <Controller
            name="status"
            control={control}
            render={({ field: f }) => (
              <Select onValueChange={f.onChange} defaultValue={f.value}>
                <SelectTrigger id="status">
                  <SelectValue placeholder="Select status" />
                </SelectTrigger>
                <SelectContent>
                  <SelectItem value="active">active</SelectItem>
                  <SelectItem value="idle">idle</SelectItem>
                  <SelectItem value="closed">closed</SelectItem>
                </SelectContent>
              </Select>
            )}
          />
          {errors.status && <p className="text-sm text-destructive">{errors.status?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create ProxySession' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default ProxySessionForm;
