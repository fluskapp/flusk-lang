// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';

import { Switch } from '@/components/ui/switch';
import { Textarea } from '@/components/ui/textarea';
import type { TelemetryEvent } from '../types/TelemetryEvent';

// ── Validation schema ──────────────────────────────────────────────────────
const telemetryEventFormSchema = z.object({
  org_id: z.number().int().optional(),
  device_id: z.string().optional(),
  trace_id: z.string().optional(),
  span_id: z.string().optional(),
  provider: z.string().optional(),
  model: z.string().optional(),
  tool_name: z.string().optional(),
  input_tokens: z.number().int().optional(),
  output_tokens: z.number().int().optional(),
  latency_ms: z.number().int().optional(),
  cost_usd: z.number().optional(),
  status_code: z.number().int().optional(),
  is_approved: z.boolean().optional(),
  metadata: z.string().optional(),
});

export type TelemetryEventFormValues = z.infer<typeof telemetryEventFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface TelemetryEventFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<TelemetryEvent>;
  onSubmit: (values: TelemetryEventFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function TelemetryEventForm({ mode, defaultValues, onSubmit, isLoading }: TelemetryEventFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<TelemetryEventFormValues>({
    resolver: zodResolver(telemetryEventFormSchema),
    defaultValues: defaultValues as TelemetryEventFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="device_id">Device_id</Label>

          <Input id="device_id" type="text" {...register('device_id')} />
          {errors.device_id && <p className="text-sm text-destructive">{errors.device_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="trace_id">Trace_id</Label>

          <Input id="trace_id" type="text" {...register('trace_id')} />
          {errors.trace_id && <p className="text-sm text-destructive">{errors.trace_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="span_id">Span_id</Label>

          <Input id="span_id" type="text" {...register('span_id')} />
          {errors.span_id && <p className="text-sm text-destructive">{errors.span_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="provider">Provider</Label>

          <Input id="provider" type="text" {...register('provider')} />
          {errors.provider && <p className="text-sm text-destructive">{errors.provider?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="model">Model</Label>

          <Input id="model" type="text" {...register('model')} />
          {errors.model && <p className="text-sm text-destructive">{errors.model?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="tool_name">Tool_name</Label>

          <Input id="tool_name" type="text" {...register('tool_name')} />
          {errors.tool_name && <p className="text-sm text-destructive">{errors.tool_name?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="input_tokens">Input_tokens</Label>

          <Input
            id="input_tokens"
            type="number"
            {...register('input_tokens', { valueAsNumber: true })}
          />
          {errors.input_tokens && <p className="text-sm text-destructive">{errors.input_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="output_tokens">Output_tokens</Label>

          <Input
            id="output_tokens"
            type="number"
            {...register('output_tokens', { valueAsNumber: true })}
          />
          {errors.output_tokens && <p className="text-sm text-destructive">{errors.output_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="latency_ms">Latency_ms</Label>

          <Input
            id="latency_ms"
            type="number"
            {...register('latency_ms', { valueAsNumber: true })}
          />
          {errors.latency_ms && <p className="text-sm text-destructive">{errors.latency_ms?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_usd">Cost_usd</Label>

          <Input
            id="cost_usd"
            type="number"
            {...register('cost_usd', { valueAsNumber: true })}
          />
          {errors.cost_usd && <p className="text-sm text-destructive">{errors.cost_usd?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="status_code">Status_code</Label>

          <Input
            id="status_code"
            type="number"
            {...register('status_code', { valueAsNumber: true })}
          />
          {errors.status_code && <p className="text-sm text-destructive">{errors.status_code?.message}</p>}
        </div>

        <div className="flex flex-row items-center justify-between rounded-lg border p-3">
          <div className="space-y-0.5">
            <Label htmlFor="is_approved">Is_approved</Label>

          </div>
          <Controller
            name="is_approved"
            control={control}
            render={({ field: f }) => (
              <Switch id="is_approved" checked={!!f.value} onCheckedChange={f.onChange} />
            )}
          />
          {errors.is_approved && <p className="text-sm text-destructive">{errors.is_approved?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="metadata">Metadata</Label>

          <Textarea id="metadata" {...register('metadata')} />
          {errors.metadata && <p className="text-sm text-destructive">{errors.metadata?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create TelemetryEvent' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default TelemetryEventForm;
