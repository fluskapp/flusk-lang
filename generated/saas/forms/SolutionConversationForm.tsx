// @generated by flusk-lang — DO NOT EDIT
import React from 'react';
import { useForm, Controller } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { z } from 'zod';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { Label } from '@/components/ui/label';



import type { SolutionConversation } from '../types/SolutionConversation';

// ── Validation schema ──────────────────────────────────────────────────────
const solutionConversationFormSchema = z.object({
  deployment_id: z.number().int().optional(),
  org_id: z.number().int().optional(),
  user_identifier: z.string().optional(),
  message_count: z.number().int().optional(),
  input_tokens: z.number().int().optional(),
  output_tokens: z.number().int().optional(),
  cost_usd: z.number().optional(),
  satisfaction: z.number().int().optional(),
  last_message_at: z.string().optional(),
});

export type SolutionConversationFormValues = z.infer<typeof solutionConversationFormSchema>;

// ── Props ──────────────────────────────────────────────────────────────────
interface SolutionConversationFormProps {
  mode: 'create' | 'edit';
  defaultValues?: Partial<SolutionConversation>;
  onSubmit: (values: SolutionConversationFormValues) => void | Promise<void>;
  isLoading?: boolean;
}

// ── Component ──────────────────────────────────────────────────────────────
export function SolutionConversationForm({ mode, defaultValues, onSubmit, isLoading }: SolutionConversationFormProps) {
  const {
    register,
    control,
    handleSubmit,
    formState: { errors },
  } = useForm<SolutionConversationFormValues>({
    resolver: zodResolver(solutionConversationFormSchema),
    defaultValues: defaultValues as SolutionConversationFormValues | undefined,
  });

  return (
    <form onSubmit={handleSubmit(onSubmit)} className="space-y-4">
        <div className="space-y-1">
          <Label htmlFor="deployment_id">Deployment_id</Label>

          <Input
            id="deployment_id"
            type="number"
            {...register('deployment_id', { valueAsNumber: true })}
          />
          {errors.deployment_id && <p className="text-sm text-destructive">{errors.deployment_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="org_id">Org_id</Label>

          <Input
            id="org_id"
            type="number"
            {...register('org_id', { valueAsNumber: true })}
          />
          {errors.org_id && <p className="text-sm text-destructive">{errors.org_id?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="user_identifier">User_identifier</Label>

          <Input id="user_identifier" type="text" {...register('user_identifier')} />
          {errors.user_identifier && <p className="text-sm text-destructive">{errors.user_identifier?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="message_count">Message_count</Label>

          <Input
            id="message_count"
            type="number"
            {...register('message_count', { valueAsNumber: true })}
          />
          {errors.message_count && <p className="text-sm text-destructive">{errors.message_count?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="input_tokens">Input_tokens</Label>

          <Input
            id="input_tokens"
            type="number"
            {...register('input_tokens', { valueAsNumber: true })}
          />
          {errors.input_tokens && <p className="text-sm text-destructive">{errors.input_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="output_tokens">Output_tokens</Label>

          <Input
            id="output_tokens"
            type="number"
            {...register('output_tokens', { valueAsNumber: true })}
          />
          {errors.output_tokens && <p className="text-sm text-destructive">{errors.output_tokens?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="cost_usd">Cost_usd</Label>

          <Input
            id="cost_usd"
            type="number"
            {...register('cost_usd', { valueAsNumber: true })}
          />
          {errors.cost_usd && <p className="text-sm text-destructive">{errors.cost_usd?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="satisfaction">Satisfaction</Label>
          <p className="text-xs text-muted-foreground">1-5 rating if collected</p>
          <Input
            id="satisfaction"
            type="number"
            {...register('satisfaction', { valueAsNumber: true })}
          />
          {errors.satisfaction && <p className="text-sm text-destructive">{errors.satisfaction?.message}</p>}
        </div>

        <div className="space-y-1">
          <Label htmlFor="last_message_at">Last_message_at</Label>

          <Input id="last_message_at" type="text" {...register('last_message_at')} />
          {errors.last_message_at && <p className="text-sm text-destructive">{errors.last_message_at?.message}</p>}
        </div>

      <Button type="submit" disabled={isLoading}>
        {isLoading ? 'Saving…' : mode === 'create' ? 'Create SolutionConversation' : 'Save Changes'}
      </Button>
    </form>
  );
}

export default SolutionConversationForm;
